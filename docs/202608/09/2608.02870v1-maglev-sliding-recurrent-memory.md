---
title: "Maglev: Sliding Recurrent Memory"
title_zh: Maglev：滑动循环记忆
authors: "Bo Liu, Qiang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02870v1"
tags: ["query:pic"]
score: 7.0
evidence: 面向长上下文Transformer的定长循环KV记忆
tldr: 循环Transformer常受限于固定内存或难以并行训练。Maglev提出双模型架构：预填充器Q用全注意力生成内存目标，解码器P用滑窗注意力与循环K/V注入生成内存，训练时以一致性损失对齐二者，推理仅需P。实验表明其验证损失与下游基准优于滑窗注意力和潜在循环基线，且参数共享可节省内存。该方法兼顾训练并行性与状态记忆，为高效序列建模提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02870v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1135, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02870v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 477, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02870v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 437, \"label\": \"Table\"}]"
motivation: 现有滑窗注意力和潜在循环Transformer在固定内存与并行训练间难以兼得，需设计一种保持并行且内存固定的循环架构。
method: 构建耦合的预填充器Q与解码器P，Q用全注意力生成内存目标，P用滑窗注意力加循环K/V注入，通过一致性损失对齐二者。
result: Maglev在验证损失和下游预训练基准上超越滑窗注意力和潜在循环Transformer基线，参数共享可减少内存占用而损失较小。
conclusion: 内存一致性训练使架构兼顾并行效率与推理性能，为循环Transformer提供了一条有效实现路径。
---

## 摘要
我们引入了\ours{}，一种具有固定大小记忆的循环Transformer架构，它推广了滑动窗口注意力，同时在训练期间保持可并行化。\ours{}由两个耦合模型组成：一个预填充器$Q$，它利用完全注意力\footnote{在实践中，我们对$Q$使用交错的全注意力和滑动窗口注意力，因为这会产生更强的性能。基本要求是$Q$比$P$更具表达力，并且能够访问完整历史。}来产生记忆目标$m'_t$，以及一个解码器$P$，它仅使用滑动窗口注意力和循环K/V注入来产生用于下一词预测的解码器记忆$m_t$。我们使用一个记忆一致性损失来训练\ours{}，该损失将$m_t$与$m'_t$对齐，从而允许推理时仅使用$P$。实验上，\ours{}在验证损失和下游预训练基准上优于滑动窗口和潜在循环Transformer基线。此外，在$P$和$Q$之间共享参数可以减少参数内存，同时保留大部分收益。

## Abstract
We introduce \ours{}, a recurrent Transformer architecture with fixed-size memory that generalizes sliding-window attention while remaining parallelizable during training. \ours{} consists of two coupled models: a prefiller $Q$, which leverages full attention\footnote{In practice, we use interleaved full and sliding-window attention for $Q$, as this yields stronger performance. The essential requirement is that $Q$ be more expressive than $P$, with access to the full history.} to produce memory targets $m'_t$, and a decoder $P$, which uses only sliding-window attention and recurrent K/V injection to produce decoder memories $m_t$ for next-token prediction. We train \ours{} with a memory consistency loss that aligns $m_t$ with $m'_t$, allowing inference to use $P$ alone. Empirically, \ours{} improves validation loss and downstream pretraining benchmarks over sliding-window and latent recurrent transformer baselines. Moreover, sharing parameters between $P$ and $Q$ reduces parameter memory while preserving most of the gains.