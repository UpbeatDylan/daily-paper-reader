---
title: "CrossPool: Efficient Multi-LLM Serving for Cold MoE Models through KV-Cache and Weight Disaggregation"
title_zh: "CrossPool: 通过KV缓存与权重分离实现冷MoE模型的高效多LLM服务"
authors: "Zhuoren Ye, Tianyu Wo, Dinghao Xue, Mingming Zhang, Yuchen Teng, Chunming Hu, Renyu Yang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24506v1"
tags: ["query:pic"]
score: 9.0
evidence: KV缓存与权重解聚实现高效多LLM服务
tldr: 冷MoE模型服务面临GPU内存浪费问题：静态权重与动态KV-cache竞争内存，且低并发下KV容量利用率低。CrossPool将FFN权重与KV-cache分离为两个GPU内存池，权重池合并冷模型，KV-cache池动态服务请求，并保持注意力局部化。结合KV-cache规划器、层流水线调度和持久内核，减少控制开销。在突发长上下文请求下，P99 TBT相比现有系统降低10.4倍，显著提升多模型服务效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24506v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 820, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 754, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 802, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 888, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 882, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 891, \"height\": 807, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24506v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24506v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24506v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 296, \"label\": \"Table\"}]"
motivation: 多个冷MoE模型服务中，静态模型权重与动态KV-cache共存于显存，且预留KV容量浪费，导致显存利用率低，长上下文支持弱。
method: CrossPool将FFN权重和KV-cache分池管理，权重池整合冷模型，KV-cache池动态共享；设计KV-cache规划器、层流水线调度及持久内核优化传输与开销。
result: 相比基于KV-cache共享的最先进多LLM服务系统，P99 TBT（token间耗时）最高降低10.4倍，支持突发长上下文请求。
conclusion: 通过权重与KV-cache的物理分离与池化，CrossPool高效利用显存，显著提升冷MoE模型服务的吞吐和长上下文能力。
---

## 摘要
新兴的LLM服务越来越多地托管多个稀疏MoE模型，但大多数模型请求稀疏且处于冷状态。这造成了GPU内存问题：模型权重是稳定的且由模型决定，而KV缓存是瞬时的且由需求决定。由于冷模型很少同时达到KV缓存需求的峰值，为每个模型预留最坏情况下的KV容量会浪费内存；相反，共享的KV缓存池可以按需供应总的活跃需求。然而，当权重和KV缓存仍处于单一的GPU内存池中时，KV缓存共享是不够的。静态权重与动态KV缓存竞争，且在冷、低并发流量下，受KV头限制的注意力仅利用了复制KV容量的一小部分，导致GPU内存利用率低和长上下文支持弱。我们提出了CrossPool，一种针对冷MoE模型的服务引擎，它将FFN权重和KV缓存分离到两个GPU内存池中：一个权重池，用于整合冷模型间的FFN权重；一个KV缓存池，用于动态服务活跃请求，同时保持注意力在KV缓存本地。CrossPool结合了KV缓存规划器和虚拟化器、层间流水线调度器（隐藏隐藏状态传输）以及具有控制降低的持久化内核，以减少CPU-GPU控制开销。通过高效的GPU内存池化，CrossPool支持突发的长上下文请求，并优于基于KV缓存的最新多LLM服务系统，将P99 TBT降低高达10.4倍。

## Abstract
Emerging LLM services increasingly host many sparse MoE models, yet most models receive sparse requests and remain cold. This creates a GPU memory problem: model weights are stable and model-determined, while KV-cache is transient and demand-determined. Because cold models rarely reach peak KV-cache demand at the same time, reserving worst-case KV capacity per model wastes memory; a shared KV-cache pool can instead provision aggregate active demand. However, KV-cache sharing is not sufficient when weights and KV-cache remain in a monolithic GPU memory pool. Static weights compete with dynamic KV-cache, and KV-head-limited attention under cold, low-concurrency traffic exposes only a fraction of replicated KV capacity, leading to low GPU memory utilization and weak long-context support. We present CrossPool, a serving engine for cold MoE models that separates FFN weights and KV-cache into two GPU memory pools: a weights pool that consolidates FFN weights across cold models, and a KV-cache pool that dynamically serves active requests while keeping attention local to KV-cache. CrossPool combines a KV-cache planner and virtualizer, a layer-wise pipeline scheduler that hides hidden-state transfers, and persistent kernels with control lowering to reduce CPU-GPU control overhead. With efficient GPU memory pooling, CrossPool underpins bursty long-context requests and outperforms the state-of-the-art kvcached-based multi-LLM serving system, reducing P99 TBT by up to $10.4\times$.

---

## 论文详细总结（自动生成）

# 论文总结：CrossPool

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在同时服务多个稀疏MoE（Mixture-of-Experts）模型时，模型权重（稳定、模型决定）与KV-cache（瞬时、需求决定）共存于同一个GPU内存池中，导致静态权重与动态KV-cache竞争内存，且低并发下受KV头限制的注意力算法（如MQA、MLA）仅能利用部分复制KV容量，造成GPU内存利用率低、长上下文支持弱。
- **整体含义**：旨在通过分离两种不同生命周期的内存对象，提升冷模型服务的内存效率，降低P99 TBT（token间耗时），支持突发长上下文请求。

## 2. 论文提出的方法论

### 核心思想
将MoE模型的FFN权重与KV-cache分离到两个独立的GPU内存池：
- **权重池**：整合多个冷模型的FFN权重（占MoE参数主导地位，如95%以上）。
- **KV-cache池**：动态服务活跃请求的KV-cache，保持注意力局部化（attention在池内执行）。

### 关键技术细节
- **KV-cache规划器与虚拟化器**：
  - 离线规划器：基于请求的prompt长度、输出长度、服务时间、到达率，通过蒙特卡洛分位数（如P95/P99）估算总KV-cache池预算，并确定每个模型的并行计划（避免过多DP副本）。
  - 在线虚拟化器：使用CUDA VMM API实现逻辑页到物理页的按需映射，暴露统一的分页KV-cache接口。
- **层间流水线调度器**：
  - 将KV-cache池和权重池之间的隐藏状态传输（hidden-state transfer）与计算重叠：一个批次在KV-cache池执行注意力时，另一个批次的隐藏状态在权重池执行FFN，以此减少乒乓开销。
- **持久化内核与控制降低**：
  - 分别捕获注意力子图和FFN子图（CUDA graph），在GPU上启动持久化内核，轮询GPU端请求队列、调度子图、启动跨池通信，避免频繁的CPU-GPU控制转换。

### 公式或算法流程
- 活跃KV需求建模：\( K_M(t) = \sum_i \kappa(M) Q_i(t - A_i) \)，其中\( Q_i(u) = \min(O_{p,i} + O_{d,i}, u/T_i \cdot O_{d,i}) \)，表示请求i在时间u的KV token量。
- 总池需求：\( K_{pool}(t) = \sum_M K_M(t) \)，使用P95/P99 trace驱动的蒙特卡洛估计作为保守目标。

## 3. 实验设计

### 使用的数据集/场景
- **工作负载**：
  - 平衡输入/输出：Vicuna ShareGPT对话数据（用于TBT测试）。
  - 长上下文：LongAlign长上下文请求（用于可扩展性测试）。
- **场景**：低RPS（0.2~1.0 RPS per model）的冷模型服务，prompt长度从4K到128K不等。

### Benchmark与对比方法
- **对比系统**：
  - **Static Partition**：SGLang原生静态放置，每个模型固定GPU。
  - **kvcached (Chimera)**：弹性GPU内存池化，但不分离权重和KV-cache。
- **测试平台**：预填充-解码分离模式，仅评估解码阶段。

### 对比方法完整列表
| 方案 | 说明 |
|------|------|
| Static Partition | 单模型独占GPU，静态MIG分区 |
| kvcached | 动态KV内存弹性管理，权重与KV共池 |
| CrossPool | 本文方法，分离FFN权重池和KV-cache池 |

## 4. 资源与算力

- **GPU型号**：5块A100 GPU，每块40 GB HBM，通过NVLink互联。
- **模型**：共同部署三个MoE模型（Qwen3-30B-A3B、GLM-4.7-Flash、DeepSeek-V2-Lite），总权重约154 GB。
- **说明**：论文仅详细描述了评估平台的配置，未提及训练或预训练所需的算力（本文属于服务优化，不涉及训练）。

## 5. 实验数量与充分性

- **主要实验组**：
  - 上下文长度可扩展性实验（图6）：对比三种方法在不同context bin下的最大RPS。
  - TBT延迟实验（图7）：在0.2~1.0 RPS下，报告三个模型的P95和P99 TBT。
  - 消融实验（表3）：分别评估层间流水线调度器和持久化内核/控制降低对吞吐的影响。
- **充分性分析**：
  - 实验覆盖了关键的上下文长度和请求率范围，消融实验验证了两种主要技术的贡献。
  - 对比方法选用了当前最先进的系统（kvcached）和经典静态分区，对比公平。
  - 不同模型（MoE、不同注意力类型）的测试增强了泛化性。
  - 不足：仅测试了三个模型，未涵盖更多类型的MoE模型或更大规模集群；未提供multi-node实验；未在真实云环境中测试。

## 6. 论文的主要结论与发现

- CrossPool在相同的5-GPU硬件预算下，相比kvcached和Static Partition，能支持更长上下文、更高RPS，避免OOM。
- TBT显著降低：在0.8 RPS时，P99 TBT降低倍数分别为Qwen3-30B-A3B: 7.6×, DeepSeek-V2-Lite: 10.4×, GLM-4.7-Flash: 7.3×。
- 消融实验表明，同时启用层间流水线调度和持久化内核可使吞吐提升2.01倍（从55.42 token/s到111.40 token/s）。
- 结论：通过将FFN权重和KV-cache物理分离并池化，CrossPool有效利用GPU内存，大幅提升冷模型服务的吞吐和长上下文支持能力。

## 7. 优点

- **设计创新**：将两种不同生命周期的内存对象（稳定的权重与瞬时的KV-cache）分离，解决算法-系统不匹配问题，思路清晰。
- **技术完整**：包含离线规划、在线虚拟化、层间流水线、持久化内核等一整套工程优化，可实用。
- **实验充分**：在多个模型、多种上下文长度、多种RPS下对比，且有消融实验验证各组件效果。
- **性能提升显著**：在低并发长上下文场景下P99 TBT降低一个数量级，记忆效率优势明显。
- **对现有系统的兼容性**：基于SGLang实现，保留大部分操作符和内核。

## 8. 不足与局限

- **实验场景有限**：仅测试了一个5-GPU节点，未评估跨节点扩展、更大规模的模型集或不同异构硬件。
- **仅覆盖解码阶段**：预填充阶段单独处理，全生命周期优化未完全展示。
- **假设冷模型流量稀疏**：若流量突然升高（变为热模型），池化策略可能失效。
- **通信开销未完全消除**：层间隐状态传输虽被重叠，但仍可能成为瓶颈，论文未讨论细粒度流水线。
- **计划阶段依赖离线采样**：准确度受采样数据质量影响，对突发流量可能不够鲁棒。
- **未包含成本分析**：仅比较了性能，未考虑跨池通信带来的额外延迟和能量开销。

（完）
