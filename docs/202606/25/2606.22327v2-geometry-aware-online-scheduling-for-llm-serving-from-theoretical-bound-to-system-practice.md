---
title: "Geometry-Aware Online Scheduling for LLM Serving: From Theoretical Bound to System Practice"
title_zh: 面向LLM服务几何感知的在线调度：从理论界到系统实践
authors: "Li Kong, Qi Qi, Yinyu Ye, Zijie Zhou"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22327v2"
tags: ["query:pic"]
score: 8.0
evidence: 几何感知调度管理KV缓存内存，加速长上下文推理
tldr: LLM推理中KV缓存内存管理是关键瓶颈。现有时间调度启发式忽略其二维时空几何增长。本文提出几何感知在线调度算法SVF及1-bit变体，将最坏竞争比从48降至3。集成到vLLM后显著降低平均和尾部延迟，为内存受限调度提供理论与实践方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22327v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1680, \"height\": 940, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1678, \"height\": 702, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22327v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1007, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1347, \"height\": 241, \"label\": \"Table\"}]"
motivation: 现有时间调度启发式未捕捉LLM推理中KV缓存的二维时空几何特性，导致内存受限时性能瓶颈。
method: 提出Smallest Volume First (SVF)算法，基于KV缓存体积调度，并证明其竞争比从48降至3。
result: SVF大幅降低平均和尾部延迟；1-bit SVF以单比特信息实现竞争性性能。
conclusion: 建立了理论严密且实践验证的几何感知调度方法，为LLM部署中的内存调度提供新思路。
---

## 摘要
对交互式大型语言模型服务的爆炸性需求凸显了键值缓存动态内存占用的管理作为推理引擎性能优化的关键领域。现代推理系统绝大多数依赖于以时间为中心的调度启发式算法，如最短作业优先。然而，它们的理论最优性植根于传统调度建模，未能捕捉到LLM推理机制特有的高度动态的二维时空几何增长。为了解决这个问题，我们通过引入最小体积优先（SVF）算法及其高效变体1-bit SVF，提出了几何感知的在线调度。理论上，我们为我们的方法提供了严格的数学基础。通过一种新颖的容量证书证明，我们将SVF在最坏情况下的竞争比从先前最优的48提高到LLM服务高并发场景下的\textbf{3}。基于这一核心突破，我们完成了一个全面的理论分类，分析了我们的算法在不同流量场景和信息可用性下的表现。实践上，我们将我们的方法作为即插即用层无缝集成到vLLM中。在Llama-3.1模型上的广泛评估展示了全面的性能提升：SVF在平均延迟和尾部延迟上均实现了显著降低，而仅需单个比特信息的1-bit SVF在吞吐量和延迟方面也取得了有竞争力的表现。这项工作为在现代LLM部署中解决内存受限的调度提供了一种理论上可靠且经验证的方法。为促进未来研究，我们的代码可在https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git获取。

## Abstract
The explosive demand for interactive Large Language Model serving has highlighted the management of the Key-Value cache's dynamic memory footprint as a critical area for performance optimization in inference engines. Modern inference systems overwhelmingly rely on time-centric scheduling heuristics, such as Shortest Job First. However, their theoretical optimality is rooted in traditional schedule modeling, failing to capture the highly dynamic, 2D spatio-temporal geometric growth specific to LLM inference mechanisms. To resolve this, we propose the geometry-aware online scheduling by introducing the Smallest Volume First (SVF) algorithm and its highly efficient variant, 1-bit SVF. Theoretically, we provide a rigorous mathematical foundation for our approach. Via a novel volume-certificate proof, we sharpen SVF's worst-case competitive ratio from the prior best of 48 towards \textbf{3} in the high-concurrency regime of LLM serving. Building upon this core breakthrough, we complete a comprehensive theoretical taxonomy analyzing our algorithms across different traffic scenarios and information availability. Practically, we seamlessly integrate our approach as a plug-and-play layer in vLLM. Extensive evaluations on Llama-3.1 models demonstrate comprehensive performance gains: SVF delivers strong reductions in both average and tail latency, while 1-bit SVF, with merely a single bit information, achieves competitive throughput and latency. This work establishes a theoretically sound and empirically proven approach for resolving memory-constrained scheduling in modern LLM deployments. To facilitate future research, our code is available at https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git.

---

## 论文详细总结（自动生成）

### 核心问题与整体含义（研究动机与背景）

- **问题**：大语言模型（LLM）推理服务中，键值（KV）缓存的内存占用随自回归解码动态增长，而现有的调度策略（如最短作业优先 SJF）只考虑一维时间指标，忽略了这种**二维时空几何特性**（内存占用随时间线性扩大）。这导致在最坏情况下 SJF 的竞争比无界，无法适应高并发、内存受限的真实部署场景。
- **意义**：本文首次将 LLM 推理调度从传统的一维时间启发式提升到**几何感知的二维调度**，为内存受限下的低延迟服务提供了理论保证与系统实践的统一框架。

### 方法论：核心思想、关键技术细节

- **核心思想**：将每个请求的内存占用建模为时空二维“体积”（volume），定义体积公式  
  `v_i = s_i * o_i + (o_i^2 + o_i) / 2`  
  其中 `s_i` 为 prompt 长度，`o_i` 为输出长度。按体积升序调度，即 **Smallest Volume First (SVF)**。
- **关键技术细节**：
  - **SVF 算法**：使用输出长度预测器（BERT-base）估计 `o_i`，实时计算体积并维护优先队列；后台异步预测避免阻塞推理关键路径。
  - **1-bit SVF**：仅使用一个二进制分类器（BERT-tiny）将请求分为“短”（`o ≤ √T`，T 为最大生成长度）和“长”，并分配理论代理长度 `O0=T^{1/4}`, `O1=T^{3/4}`，计算代理体积后调度。极大降低预测开销。
- **理论贡献**：
  - 提出**体积容量证书**证明方法，将 SVF 在最坏突发到达下的竞争比从先前最优的 48 降至 **3**（当单请求内存占比 α→0）。
  - 在泊松到达模型下，给出 SVF 和 1-bit SVF 的期望竞争比界，且 1-bit SVF 的退化量随阈值指数衰减。

### 实验设计：数据集、场景、基准与对比方法

- **数据集**：
  - LMSYS-Chat：高并发交互式聊天流量。
  - LongBench：长上下文、内存密集型任务。
- **场景**：
  - **突发到达（Burst）**：所有请求同时到达，测试最坏情况抗阻塞能力。
  - **泊松到达（Poisson）**：以 QPS 2, 8, 16, 32, 64, 128 模拟稳态负载。
- **对比方法**：
  - 基本调度器：FCFS, SJF, SVF, 1-bit SVF。
  - 基于真实长度的理想上界：Oracle-SJF, Oracle-SVF（仅用于消融验证）。
- **评估指标**：每 token 平均延迟（Avg Latency）、P95 尾部延迟、吞吐量（tok/s）。

### 资源与算力

- **硬件**：单节点配备 **8× NVIDIA A100 (80GB) GPU**。
- **模型部署**：
  - Llama-3.1-8B-Instruct：单 GPU。
  - Llama-3.1-70B-Instruct：8 GPU 张量并行。
- 精度为 FP16/BF16，最大上下文 65536 tokens，每请求最大输出 4096 tokens。
- 预测器训练时长未明确说明，但实验部分提及使用离线数据训练，并报告预测器开销（≤0.06% 端到端时间）。

### 实验数量与充分性

- 实验覆盖**两大模型**（8B、70B）、**两大数据集**、**两种到达模式**（突发 + 六种 QPS 的泊松），共生成 **4×2×2=16 组主表/图**（表2，图2，图3）。
- 包含**理论验证消融**：对比 Oracle-SJF 与 Oracle-SVF，证明 1D 时间指标的本质缺陷。
- 包含**系统开销实验**：表3显示预测器时间占比极低。
- 总体实验设计**全面且公平**：对比基础调度器、设置理想上界、覆盖典型负载与极端情况，消融实验合理。唯一不足是未与其他更复杂的系统级执行优化（如 proactive swapping）直接对比，但作者指出 SVF 可作为上层策略叠加。

### 主要结论与发现

1. **SVF** 在所有场景下均取得**最低平均延迟**，且在多数情况下取得最低 P95 尾部延迟和更高吞吐量。
2. **1-bit SVF** 仅需一个比特信息即可显著优于 FCFS，并在部分内存密集型场景下超越 SJF，验证了轻量级设计的有效性。
3. **理论验证**：Oracle-SVF 持续优于 Oracle-SJF，甚至在 LongBench 上非先知 SVF 已低于 Oracle-SJF，证明 2D 几何指标是本质范式转变。
4. **理论界**：突发下 SVF 竞争比可达 **3**（α→0），泊松下期望竞争比与系统利用率、几何分布参数相关，且 1-bit SVF 的额外代价随阈值指数衰减。

### 优点

- **理论贡献突出**：首次将 LLM 推理调度竞争比从前沿的 48 降至 3，且证明方法新颖（体积容量证书），避免了传统松弛分析带来的 slack。
- **算法轻量化**：SVF 可即插即用，1-bit SVF 极低开销，实际部署友好。
- **实验验证充分**：覆盖多种模型、数据集、负载模式，并设计了 Oracle 对比以验证理论本质。
- **代码开源**：增强可复现性。

### 不足与局限

- **单 worker 限制**：当前实现仅针对单 GPU 或单节点（张量并行），未扩展到多 GPU 集群；作者已指出未来方向。
- **预测依赖**：即使 1-bit SVF 开销极小，仍需训练分类器，可能带来额外维护成本；极端负载下预测器质量可能下降，但理论界提供了鲁棒性保障。
- **未与系统级优化直接比较**：实验仅对比纯调度器，未与如 proactive swapping、KV cache offloading 等执行机制联合评估；但作者声明 SVF 可叠加，需后续工作验证。
- **竞争比紧性**：理论界为最坏情况上界，实际性能可能远好；但证明本身未给出下界，无法确认紧性。
- **公平性讨论**：虽然 P95 显示 SVF 未极端饿死长请求，但文中未提供具体的公平性指标（如 JFJ 公平指数）。

（完）
