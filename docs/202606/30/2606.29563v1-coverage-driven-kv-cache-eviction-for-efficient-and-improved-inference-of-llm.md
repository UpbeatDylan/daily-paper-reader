---
title: Coverage-Driven KV Cache Eviction for Efficient and Improved Inference of LLM
title_zh: 覆盖驱动的KV缓存淘汰策略以实现高效且改进的LLM推理
authors: "Shuvendu Roy, Mengyao Zhai, Hossein Hajimirsadeghi, Golnoosh Samei"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29563v1"
tags: ["query:pic"]
score: 8.0
evidence: 覆盖驱动的KV缓存驱逐策略，提高LLM推理效率
tldr: 大语言模型推理时KV缓存占用大量内存，现有驱逐策略虽利用注意力稀疏性减少内存，但导致性能下降。研究发现性能下降与唯一token覆盖率降低有关，并理论证明低覆盖率限制输入输出互信息，损害预测精度。为此提出K-VEC，一种覆盖感知的KV缓存驱逐策略，优先保证token覆盖率，并引入跨头与跨层覆盖模块增强保留。在LongBench 16个子集上，相同驱逐率和内存约束下性能提升高达10.35点，验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 520, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 404, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1699, \"height\": 172, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1355, \"height\": 956, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 790, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1679, \"height\": 1004, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1026, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1674, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1683, \"height\": 406, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 329, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 471, \"height\": 157, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 549, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 556, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 801, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29563v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1492, \"height\": 548, \"label\": \"Table\"}]"
motivation: 现有KV缓存驱逐策略因降低唯一token覆盖率导致长上下文推理性能显著下降，需解决覆盖率与内存缩减间的矛盾。
method: 提出K-VEC：覆盖感知驱逐策略，通过跨头与跨层覆盖模块优先保留高覆盖token，维持互信息并提升预测精度。
result: 在LongBench 16个子集上，相同驱逐率和内存约束下相比现有方法性能提升最高10.35点。
conclusion: K-VEC有效缓解因驱逐导致的覆盖率下降，兼顾内存效率与推理精度，适用于资源受限场景的LLM部署。
---

## 摘要
大型语言模型（LLM）在问答和摘要等复杂任务中表现出色，这得益于其处理长上下文输入的能力。然而，部署LLM成本高昂，不仅因为自注意力机制的二次复杂度和自回归生成带来的高计算需求，还因为推理过程中存储键值（KV）缓存所需的大量内存开销。为降低内存成本，现有的KV缓存淘汰策略利用注意力稀疏性选择性地存储一部分令牌。虽然减少了内存占用，但此类方法在性能上出现显著下降，尤其是在需要长上下文推理的任务中。我们识别出性能下降与唯一令牌覆盖率的降低有关。此外，我们从理论上证明，降低的覆盖率限制了输入和输出之间的互信息，从而损害了预测准确性。为此，我们提出了K-VEC，一种新颖的覆盖感知KV缓存淘汰策略，在淘汰缓存中的令牌时优先考虑令牌覆盖率。K-VEC引入了跨头和跨层的覆盖模块，以增强跨注意力头和模型层的令牌保留，缓解低覆盖率导致的性能退化。在16个LongBench子集上的评估表明，在相同的淘汰率和内存约束下，K-VEC相较于现有方法表现出高达10.35个百分点的提升。全面的评估验证了我们方法的有效性，并展示了其在资源受限环境中高效部署LLM的潜力。

## Abstract
Large language models (LLMs) excel at complex tasks like question answering and summarization, thanks to their ability to handle long-context inputs. However, deploying LLMs is costly, not only due to the high computational demands of quadratic complexity of self-attention and auto-regressive generation, but also because of the significant memory overhead required for storing the key-value (KV) cache during inference. To reduce the memory cost, existing KV-cache eviction strategies leverage the sparsity in attention to selectively store a subset of tokens. While reducing the memory footprint, such approaches show a considerable drop in performance, especially in tasks that require long-context reasoning. We identify that the drop in performance is linked to a reduction in the coverage of unique tokens. Additionally, we theoretically show that reduced coverage limits the mutual information between inputs and outputs, thereby impairing predictive accuracy. To this end, we introduce K-VEC, a novel coverage-aware KV-cache eviction strategy that prioritizes token coverage while evicting tokens in the cache. K-VEC introduces a cross-head and a cross-layer coverage module to enhance token retention across attention heads and model layers, mitigating performance degradation caused by low coverage. Evaluated on 16 LongBench subsets, K-VEC exhibit up to 10.35 points improvement over the existing methods under the same eviction rate and memory constraint. Comprehensive evaluations validate the effectiveness of our approach and demonstrate its potential for efficient LLM deployment in resource-constrained settings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）在推理时存储键值（KV）缓存会带来巨大的内存开销，现有KV缓存淘汰策略（如SnapKV、H2O、PyramidKV等）通过利用注意力稀疏性选择性保留部分令牌来减少内存，但在高淘汰率下，尤其是长上下文推理任务中性能显著下降。
- **根源识别**：论文通过初步分析发现，性能下降与**唯一令牌覆盖率（coverage）** 的降低紧密相关。覆盖率指在KV缓存中至少出现一次（跨头或跨层）的独特令牌数量。现有方法独立计算每个头和每层的淘汰分数，导致不同头/层倾向于保留相似（如靠近序列末尾）的令牌，从而大幅降低总体覆盖率。
- **理论支撑**：利用信息瓶颈理论建立联系——低覆盖率限制了输入与输出之间的互信息，进而损害预测精度。形式化推导表明，覆盖率C与性能之间存在对数关系：要达到目标性能P0，所需覆盖率为C ≥ exp( (1/α)φ⁻¹(P0) )，从而解释了性能崩溃的必然性。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **K-VEC**（KV Cache Eviction with Coverage）是一种**覆盖感知的KV缓存淘汰策略**，在淘汰时主动提升令牌覆盖率，同时保持令牌重要性。通过两个模块分别处理**跨头冗余**和**跨层冗余**，确保不同头/层保留更多样化的重要令牌。

### 关键技术细节
1. **跨头覆盖模块**：
   - 基于已有淘汰分数（窗口注意力平均，窗口大小O=16），每个头选择top-B令牌。
   - 计算每个头的注意力分数标准差σ，选出标准差最低的δ个“不聚焦”头（它们对令牌区分度低，倾向于随机选择）。
   - 对这些选出的头，使用更大的观察窗口O'（实验中O'=32）重新计算淘汰分数，使其关注更广泛的上下文，从而鼓励选择不同令牌。
   - 参数：δ=3（头数），O'=32。

2. **跨层覆盖模块**：
   - 定义token t到第ℓ层为止的覆盖率 coverage_ℓ,t = n_t / (ℓ+1)，其中n_t是该token在之前各层中被选中的次数。
   - 计算全局重要性 I_ℓ,t = (1/O) Σ_{q=T-O+1}^{T} max_h A_ℓ,h,q,t（取所有头的最大注意力，窗口平均）。
   - 组合成焦点分数 focus_ℓ,t = I_ℓ,t * (1 - coverage_ℓ,t)，鼓励选择全局重要但之前未被充分覆盖的token。
   - 最终淘汰分数调整为 P'_ℓ,h,t = P_ℓ,h,t + λ * focus_ℓ,h,t，其中λ=1.0控制覆盖与重要性平衡。
   - 为避免丢失当前层特别重要的令牌，保留 top-β·B 个原始分数最高的令牌（β=25%），强制它们不被淘汰（分数设为1.0）。
   - 算法流程：输入注意力分数，依次计算初始淘汰分数、选定头扩大窗口、计算全局重要性和覆盖率、生成焦点分数、调整分数、强制保留高重要令牌，输出最终淘汰分数。

## 3. 实验设计

- **数据集**：使用**LongBench**的16个子集，涵盖单文档QA（NarrativeQA, Qasper, MultiFieldQA-en）、多文档QA（HotpotQA, 2WikiMQA, Musique）、摘要（GovReport, QMSum, MultiNews）、少样本学习（TREC, TriviaQA, SAMSum）、合成任务（PassageCount, PassageRetrieval）和代码（Lcc, RB-P）。平均序列长度6,711词。
- **基准模型**：Llama-3.1-8B-Instruct（采用分组查询注意力GQA）。
- **对比方法**：
  - 现有SOTA淘汰方法：SnapKV、PyramidKV、AdaKV（Ada-Pyramid和Ada-SnapKV）。
  - 滑窗方法：StreamingLLM（SLM）。
  - 完全无淘汰的Full Cache作为上界。
  - 额外比较：HeadKV、GemFilter（部分子集）。
- **评估设置**：四种缓存预算B=128,256,512,1024个令牌，使用官方评估指标（F1、准确率等）。
- **实现细节**：K-VEC在预填充阶段逐层应用，默认超参数λ=1.0, β=25%, δ=3, O=16, O'=32。

## 4. 资源与算力

- 论文明确所有实验在**单块NVIDIA A100 80GB GPU**上完成。
- 未说明使用的GPU数量、训练时长或总计算量，也未提及是否运行多次取平均。由于是推理阶段优化，可能仅需单次推理评估，计算成本主要来自预填充阶段增加的开销。
- 论文提供了推理时间对比（表9）：随输入长度增加，K-VEC预填充稍慢（如输入10000 token时比SnapKV慢约1秒），但解码阶段速度相当，总时间随输出长度增长而趋于一致。

## 5. 实验数量与充分性

- **实验规模**：
  - 主结果（表1）：在16个数据集、4种缓存预算（共64个配置）下对比5种方法（Full Cache, SLM, Pyramid, SnapKV, Ada-Pyramid, Ada-SnapKV）及K-VEC。
  - 额外对比（表2、表3）：与HeadKV、GemFilter在部分子集上比较。
  - 消融实验（表10）：6个方面（主消融、观察窗口大小、层覆盖权重、保留比例、调整头数、头选择标准），每个变体在Qasper子集（B=256）上测试。
  - 覆盖分析与计算复杂度分析（表6、7、8、9）。
  - 跨模型泛化：Qwen2.5-7B-Instruct（表5，B=128和1024）。
  - Needle-in-a-Haystack测试（表4）。
- **客观性**：结果直接引用或复现自SnapKV/AdaKV论文设置，对比公平。消融实验覆盖各模块和关键超参数，验证了每个组件的贡献。
- **充分性**：实验覆盖多种任务类型、不同缓存预算、多个模型和对比方法，消融全面。但缺少更多模型族（如仅测试了Llama和Qwen两个系列）和更长序列（超过10k token）的验证。

## 6. 主要结论与发现

- **覆盖率是关键因素**：性能下降与覆盖率降低强相关，理论分析建立了互信息与覆盖率的对数关系。
- **K-VEC显著提升性能**：在B=128时平均提升1.61点，最高达10.35点（Qasper子集）。在所有预算和任务上一致超越现有方法。
- **跨头与跨层模块均有效**：消融显示两者分别贡献约1.35和5.09点（Qasper, B=256），组合效果最优。
- **计算效率可接受**：内存占用与现有方法相同，解码速度持平，预填充阶段略有增加（约1秒/10k token输入），但长生成任务下可忽略。
- **广泛适用**：在不同模型（Llama-3.1, Qwen2.5）和任务上均表现鲁棒。

## 7. 优点

1. **理论支撑扎实**：首次从信息瓶颈角度严格论证覆盖率对预测性能的影响，为覆盖感知策略提供了理论依据。
2. **方法设计简洁有效**：跨头覆盖利用注意力分数标准差识别不聚焦头并扩大窗口，跨层覆盖动态平衡全局重要性与已覆盖程度，计算量小且易于集成到现有流程。
3. **实验全面且对比公平**：遵循现有文献的标准设置，在多个budget和数据集上与多种SOTA方法进行对比，消融验证充分。
4. **实际部署友好**：无需修改模型架构，仅需在预填充阶段增加轻量计算，内存占用和解码速度与基线相同，适合资源受限场景。
5. **可视化清晰**：通过图3、4直观展示了覆盖差异，增强了结论可信度。

## 8. 不足与局限

1. **预填充阶段开销**：虽然作者声明开销可忽略，但在短输入/短输出场景下，预填充额外计算可能导致相对明显的延迟增加。
2. **覆盖率未达最优**：方法不追求100%覆盖，因为部分令牌可能无关。但如何动态确定最优覆盖阈值尚未深入。
3. **模型与任务泛化有限**：仅验证了Llama-3.1-8B和Qwen2.5-7B两个模型，且LongBench最大序列约10k token，未测试超长上下文（如128k token）或编解码模型。
4. **超参数依赖**：δ、λ、β、O'等超参数需针对具体模型和预算调整，论文未提供自适应策略，通用性可能受限。
5. **缺乏方差统计**：未报告多次运行的标准差，无法判断结果稳定性；部分实验可能为单次运行。
6. **理论假设简化**：假设令牌贡献可加且等信息量，实际任务中令牌重要性差异很大，理论界可能不够紧。
7. **与模型训练的兼容性**：K-VEC作为推理阶段优化，是否会影响微调或适配下游任务未讨论。

（完）
