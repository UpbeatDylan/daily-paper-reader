---
title: "CrossPool: Efficient Multi-LLM Serving for Cold MoE Models through KV-Cache and Weight Disaggregation"
title_zh: CrossPool：通过KV缓存和权重解聚实现高效的冷MoE模型多LLM服务
authors: "Zhuoren Ye, Tianyu Wo, Dinghao Xue, Mingming Zhang, Yuchen Teng, Chunming Hu, Renyu Yang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24506v2"
tags: ["query:pic"]
score: 9.0
evidence: 针对冷门MoE模型的KV缓存池化与权重分离优化
tldr: 冷MoE模型因权重与KV缓存竞争GPU内存，导致利用率低、长上下文支持弱。CrossPool将FFN权重和KV缓存分离到两个GPU内存池，结合KV缓存规划器、层间流水线调度和持久化内核，实现高效共享。相比现有系统，P99 TBT降低最多10.4倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24506v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 820, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 754, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 802, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 888, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 882, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24506v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 891, \"height\": 807, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24506v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24506v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24506v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 296, \"label\": \"Table\"}]"
motivation: 冷MoE模型权重稳定而KV缓存动态，共占GPU内存导致浪费和长上下文能力受限。
method: 分离FFN权重与KV缓存为独立GPU池，通过KV缓存规划器、虚拟化、层间流水线调度及持久化内核减少开销。
result: 在突发长上下文请求下，P99 TBT相比SOTA基于KV缓存的多LLM服务系统降低最多10.4倍。
conclusion: CrossPool通过内存池分离和高效调度，显著提升冷MoE模型的服务质量与资源利用率。
---

## 摘要
新兴的LLM服务越来越多地托管多个稀疏MoE模型，但大多数模型请求稀疏且处于冷状态。这造成了GPU内存问题：模型权重是稳定且由模型决定的，而KV缓存是瞬态且由需求决定的。由于冷模型很少同时达到KV缓存需求的峰值，为每个模型预留最坏情况的KV容量会浪费内存；共享KV缓存池可以按需供应聚合的活动需求。然而，当权重和KV缓存仍然位于单一的GPU内存池中时，KV缓存共享并不足够。静态权重与动态KV缓存竞争，而在冷、低并发流量下，受KV头限制的注意力仅暴露了复制的KV容量的一小部分，导致GPU内存利用率低和长上下文支持弱。我们提出了CrossPool，一种针对冷MoE模型的服务引擎，它将FFN权重和KV缓存分离到两个GPU内存池中：一个权重池，用于整合冷模型的FFN权重；一个KV缓存池，动态服务于活动请求，同时使注意力局部化在KV缓存上。CrossPool结合了KV缓存规划器和虚拟化器、一个隐藏状态传输的逐层流水线调度器，以及通过控制降级减少CPU-GPU控制开销的持久化内核。通过高效的GPU内存池化，CrossPool支持突发的长上下文请求，并优于基于kvcache的最新多LLM服务系统，将P99 TBT降低多达10.4倍。

## Abstract
Emerging LLM services increasingly host many sparse MoE models, yet most models receive sparse requests and remain cold. This creates a GPU memory problem: model weights are stable and model-determined, while KV-cache is transient and demand-determined. Because cold models rarely reach peak KV-cache demand at the same time, reserving worst-case KV capacity per model wastes memory; a shared KV-cache pool can instead provision aggregate active demand. However, KV-cache sharing is not sufficient when weights and KV-cache remain in a monolithic GPU memory pool. Static weights compete with dynamic KV-cache, and KV-head-limited attention under cold, low-concurrency traffic exposes only a fraction of replicated KV capacity, leading to low GPU memory utilization and weak long-context support. We present CrossPool, a serving engine for cold MoE models that separates FFN weights and KV-cache into two GPU memory pools: a weights pool that consolidates FFN weights across cold models, and a KV-cache pool that dynamically serves active requests while keeping attention local to KV-cache. CrossPool combines a KV-cache planner and virtualizer, a layer-wise pipeline scheduler that hides hidden-state transfers, and persistent kernels with control lowering to reduce CPU-GPU control overhead. With efficient GPU memory pooling, CrossPool underpins bursty long-context requests and outperforms the state-of-the-art kvcached-based multi-LLM serving system, reducing P99 TBT by up to 10.4x.

---

## 论文详细总结（自动生成）

以下是对论文《CrossPool: Efficient Multi-LLM Serving for Cold MoE Models through KV-Cache and Weight Disaggregation》的详细中文总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在托管大量稀疏 MoE（混合专家）模型的服务集群中，大多数模型处于“冷”状态（请求稀疏），导致 GPU 内存利用严重失衡。具体表现为：
  - 模型权重（Weights）是**稳定且模型决定**的，必须常驻 GPU 显存；
  - KV 缓存（KV-cache）是**瞬态且需求决定**的，只在请求活跃时占用，请求结束后释放。
  - 在传统的统一 GPU 内存池中，静态权重与动态 KV 缓存相互竞争，且 KV 头数有限的注意力算法（如 MQA、MLA）在低并发下只能暴露部分复制 KV 容量，导致**有效 GPU 内存利用率低**和**长上下文支持弱**。
- **整体含义**：通过分离权重的内存与 KV 缓存的内存，并设计高效共享策略，可以显著提升冷 MoE 模型的服务质量和资源效率，支撑突发长上下文请求。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将 FFN（前馈网络）权重和 KV 缓存解耦为两个独立的 GPU 内存池：
  - **权重池（Weights Pool）**：整合所有冷 MoE 模型的 FFN 权重（占参数绝大部分）；
  - **KV 缓存池（KV-cache Pool）**：动态服务于活跃请求，注意力模块本地访问 KV 缓存。
  - 池间仅传输 hidden states（而非 KV 张量），传输量与上下文长度无关。

- **关键技术**：
  1. **KV 缓存规划器与虚拟化器（KV-cache Planner & Virtualizer）**：
     - 离线阶段：根据 trace 数据（提示长度、输出长度、服务时间、到达率等）通过蒙特卡洛分位数（P95/P99）估计共享 KV 缓存池的页面预算，并为每个模型选择并行计划（避免不必要的 DP 副本）。
     - 在线阶段：利用 CUDA VMM API 将逻辑 KV 地址空间映射到物理页面，注意力操作通过虚拟化分页接口工作。
  2. **逐层流水线调度器（Layer-wise Pipeline Scheduler）**：
     - 维护两个在飞行批次，一个在 KV 缓存池执行注意力，另一个在权重池执行 FFN，通过重叠计算与 hidden states 传输隐藏池间通信开销。
     - 支持不同模型、不同层数的混合调度，批次完成后立即取新请求，避免全局层屏障。
  3. **持久化内核与控制降级（Persistent Kernels & Control Lowering）**：
     - 分别捕获注意力与 FFN 的子图（CUDA Graph），编译后交由 GPU 驻留持久化内核执行。
     - 持久化内核在 GPU 上轮询请求队列、调度子图、发起通信、更新批次状态，避免 CPU-GPU 频繁控制切换。

- **公式逻辑**（文字说明）：
  - 第 \(i\) 个请求在年龄 \(u\) 时的活跃 KV tokens 为 \(Q_i(u)\)，其增长受提示长度、输出长度和驻留时间控制（式1）。
  - 模型 \(M\) 在时刻 \(t\) 的 KV 缓存需求 \(K_M(t)\) 为各请求贡献之和乘以每 token KV 字节数 \(\kappa(M)\)（式1）。
  - 总池需求 \(K_{pool}(t)\) 为所有模型之和（式2），规划器以 trace 驱动的 P95/P99 分位数作为保守预算。

---

### 3. 实验设计

- **使用的数据集 / 场景**：
  - **ShareGPT（Vicuna unfiltered）**：平衡输入输出的对话数据集，用于 TBT（Token间时间）基准测试。
  - **LongAlign**：长上下文数据集，用于评估上下文长度可扩展性。
- **基准方法（Baselines）**：
  - **Static Partition**：SGLang 原生静态放置，每模型独占固定 GPU，使用静态 MIG 分区。
  - **kvcached（即 Chimera）**：弹性 GPU 内存池化，权重与 KV 缓存共池，动态管理 KV 内存。
- **被评估模型**：三个 MoE 模型复合部署：
  - Qwen3-30B-A3B
  - GLM-4.7-Flash
  - DeepSeek-V2-Lite
- **硬件平台**：5 × A100（40GB HBM）服务器，NVLink 互联。
- **主要指标**：
  - **上下文长度可扩展性**：各系统能支持的最大聚合请求率（RPS）随上下文长度增加的变化。
  - **TBT（P95 和 P99）**：衡量解码延迟。
  - **消融实验**：聚合解码输出吞吐量（token/s）用以评估两个机制（流水线调度与持久化内核）的贡献。

---

### 4. 资源与算力

- **硬件**：5 块 NVIDIA A100 GPU，每块 40GB HBM，通过 NVLink 连接。
- **运行时**：解码阶段使用上述 5 GPU；预填阶段采用分离的时域复用引擎（基于 Aegaeon），实验聚焦解码端。
- **训练时长**：论文未提及训练阶段，只关注推理服务，因此无训练算力说明。

---

### 5. 实验数量与充分性

- **实验组数**：
  - **上下文可扩展性实验（图6）**：对每个模型（Qwen3-30B-A3B 和 GLM-4.7-Flash）在不同上下文长度 bin（4K-8K 至 128K）下比较最大 RPS。
  - **TBT 性能实验（图7）**：对三个模型分别测试 RPS 从 0.2 到 1.0 共 5 个点，每个点汇报 P95 和 P99 TBT。
  - **消融实验（表3）**：4 种组合（两个机制开关）的吞吐量对比。
- **充分性与公平性**：
  - 覆盖了不同负载（平衡与长上下文）和不同模型，对比了当前最先进的系统。
  - 使用相同硬件预算（5 GPU）并给出具体放置策略。
  - 未进行多节点或多 GPU 组规模扩展实验；也未测试热模型情况；仅使用三个 MoE 模型，种类有限，但论文定位为冷模型场景，合理。

---

### 6. 论文的主要结论与发现

- **主要结论**：CrossPool 通过分离 FFN 权重与 KV 缓存为独立 GPU 内存池，显著提升了冷 MoE 模型场景下的共享 KV 缓存容量和长上下文支持能力。
- **具体发现**：
  - 在长上下文负载下，Static Partition 和 kvcached 因 KV 容量耗尽无法处理较长序列时，CrossPool 仍能保持正 RPS。
  - 在平衡负载下，CrossPool 的 P99 TBT 相比 kvcached 最高降低 10.4×（DeepSeek-V2-Lite at 0.8 RPS），相比 Static Partition 也接近或更优。
  - 消融实验表明，流水线调度和持久化内核两机制结合提升吞吐量 2.01 倍（从 55.42 到 111.40 token/s），且两者互补。

---

### 7. 优点

- **方法创新性**：首次在冷 MoE 模型服务中系统性地将权重和 KV 缓存按生命周期解耦为独立内存池，解决了算法-系统失配问题。
- **技术完整性**：从离线规划（蒙特卡洛分位数）到在线调度（层间流水线、持久化内核）形成完整方案，兼顾效率与鲁棒性。
- **实验设计**：不仅对比 TBT 和吞吐量，还专门评估了上下文长度可扩展性，突出了核心优势。
- **实现细节**：基于 SGLang 实现，保持了现有算子不变性，可复用已有优化（如 FlashAttention、FlashInfer），降低了工程成本。

---

### 8. 不足与局限

- **通信开销未完全隐藏**：Attention→FFN 和 FFN→Attention 的 hidden states 传输仍在关键路径上，虽然通过流水线重叠部分计算，但并非完全消除。
- **流水线不平衡**：不同 MoE 模型的注意力/FFN 计算时间差异可能导致较慢模型拖累整体，论文建议在放置时分组相似性能的模型，但未实验验证。
- **适用场景限定**：针对冷 MoE 模型，未讨论热模型或密集模型；仅评估了三个 MoE 模型，扩展性需更多模型验证。
- **依赖高速互联**：池间通信依赖 NVLink/NVSHMEM，若使用 PCIe 等慢速互联可能成为瓶颈。
- **未进行多节点扩展实验**：仅使用 5 GPU 单节点，实际生产环境可能更大规模，系统开销和调度复杂度需进一步验证。
- **缺乏对预填阶段的优化分析**：论文明确聚焦解码阶段，预填使用外部引擎，但整体端到端延迟未完整评估。

---

（完）
