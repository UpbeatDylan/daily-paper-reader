---
title: Recency/Frequency Adaptive KV Caching for Large Language Model Serving
title_zh: 面向大语言模型服务的近因/频率自适应KV缓存
authors: "Yang Shen, Meghana Madhyastha, Robert Underwood, Bogdan Nicolae, Randal Burns"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21238v1"
tags: ["query:pic"]
score: 9.0
evidence: 自适应KV缓存用于LLM服务，提升命中率和首Token延迟
tldr: "大模型推理中的KV缓存面临工作负载多样导致的缓存冲突问题。现有LRU策略无法区分近期与频繁访问模式。本文提出自适应缓存，动态分配近期与频繁KV块的缓存空间。在合成文档问答中缓存命中率提升10.8%，首Token时延降低12.6%，真实对话中也有2%以上改善。方法简洁可解释，兼容批处理场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21238v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1762, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 859, \"height\": 243, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1655, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1654, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1657, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 855, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1771, \"height\": 618, \"label\": \"Figure\"}]"
motivation: 现有LRU缓存策略在多样化推理工作负载下导致不同任务相互冲刷缓存，命中率低。
method: 集成自适应缓存，动态权衡近期与频繁出现KV块的空间分配。
result: "合成文档问答中缓存命中率提升10.8%，TTFT降低12.6%；真实对话中分别提升2.1%和2.0%。"
conclusion: 提出高效自适应KV缓存策略，提升多种工作负载下的缓存效率，且可解释性强。
---

## 摘要
键值（KV）缓存是一种加速大语言模型推理和生成的高效技术。推理工作负载庞大且多样，导致难以有效缓存。现有缓存管理策略采用最近最少使用策略来驱逐缓存块。然而，LRU会导致多个不相关的工作负载相互冲刷缓存。为了解决这个问题，我们集成了自适应缓存，在最近和频繁出现的KV块之间动态分配缓存空间。评估表明，在合成文档问答工作负载上，与朴素的vLLM相比，它使KV缓存命中率提高最多10.8%，首个令牌生成时间降低最多12.6%；在真实对话工作负载上，分别提高2.1%和2.0%。该方法能很好地推广到批量推理，并表现出清晰的可解释性，同时有效适应多样化的负载。

## Abstract
Key-value (KV) caching is a powerful technique for accelerating large language model inference and generation. Inference workloads are large and diverse, which makes them difficult to cache effectively. Existing cache management strategies adopt the least-recently-used policy for evicting cache blocks. However, LRU leads to multiple unrelated workloads flushing each other's caches. To address this, we integrate adaptive caching that dynamically allocates cache space between recently and frequently occurring KV blocks. Evaluations show that it improves the KV cache hit rate by up to 10.8% and reduces time to first token by up to 12.6% over naive vLLM on synthetic document question answering workloads, and 2.1% and 2.0% respectively on real-world conversation workloads. The method generalizes well to batch inference and demonstrates clear interpretability while effectively accommodating diverse workloads.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）推理中，KV缓存是加速解码的关键技术。现有主流系统（如vLLM）多采用最简单的LRU（最近最少使用）驱逐策略。
- **核心问题**：真实推理工作负载（如文档问答、多轮对话）往往同时呈现近期访问模式和频繁访问模式（如热门文档、活跃用户）。LRU策略无法区分这两种模式，导致不同请求之间相互冲刷缓存，命中率低，TTFT（首令牌延迟）高。
- **研究动机**：提出一种自适应缓存策略，在近期与频繁KV块之间动态分配缓存空间，以更好适应混合访问模式，提升缓存命中率和系统性能。

## 2. 方法论

- **核心思想**：将经典的自适应替换缓存（ARC）集成到vLLM的KV缓存管理中。ARC通过双队列 + 幽灵缓存自动实现近期-频繁的动态平衡。
- **关键技术细节**：
  - 缓存逻辑上分为**低频率队列 L1**（最近访问一次）和**高频率队列 L2**（多次访问）。L1 = T1 + B1，L2 = T2 + B2，其中T1/T2为物理缓存块，B1/B2为幽灵缓存（仅存元数据，不占显存）。
  - **命中处理**：命中T1的块被移入T2的MRU端；命中B2表示块曾多次访问但被驱逐，需重新加载并放入T2，同时调整队列大小。
  - **动态分区**：幽灵缓存中的命中作为反馈信号，自适应调整T1和T2的大小：若B1命中增多，则增加T1配额；若B2命中增多，则增加T2配额。ARC为参数无关。
  - 所有驱逐决策在KV块级别进行（块大小为16 tokens），与vLLM的分页注意力机制自然对齐。

## 3. 实验设计

- **数据集/场景**：
  - **文档问答**：使用QuALITY（381篇文档，761问题）和WikiQA（3,047问题）两个长文本阅读理解数据集。通过窗口化Zipf采样（exponent=1.0，窗口512请求，共3,072请求）生成合成工作负载，模拟热门文档的局部和偏移。
  - **多轮对话**：使用Qwen-Bailian匿名数据集Trace A（约43,058个真实请求，平均输入长度2.3k tokens），反映真实生产环境对话场景。
- **Benchmark与对比方法**：
  - **基线**：vLLM默认的LRU驱逐策略。
  - **消融对照**：DBL（ARC的简化版本，采用固定1:1比例分割两个队列），用于隔离自适应分区带来的额外增益。
- **评估指标**：KV缓存命中率、平均TTFT。

## 4. 资源与算力

- 模型：Qwen3-14B（典型14B规模）。
- 硬件：单张NVIDIA H100 PCIe GPU，80GB显存，运行在Lambda GPU Cloud。
- 文中未明确说明总实验时长或具体算力消耗。
- 注意：所有实验在单GPU离线推理模式下进行，未涉及多GPU或分布式设置。

## 5. 实验数量与充分性

- **实验组数**：
  - 文档QA：两种数据集（QuALITY和WikiQA），每种报告不同缓存大小下的命中率和TTFT（图3、4）。
  - 对话：在Trace A上报告不同缓存大小下的性能（图5）。
  - 批量推理：在QuALITY上，固定缓存20K tokens，测试不同batch size（1/4/8/16/32）下的表现（图6）。
  - 自适应分析：展示T1比例随请求时间的变化（图7），覆盖两个数据集和多种缓存容量。
- **充分性**：实验覆盖了不同工作负载（合成+真实）、不同缓存容量、不同batch size，并包含消融实验（DBL对比LRU和ARC）。实验设计相对全面且公平（使用相同基准配置）。但仅在单一模型和单GPU上评估，缺乏跨模型和多节点验证。

## 6. 主要结论与发现

- **ARC显著优于LRU**：
  - 文档QA：命中率提升1.2%-10.8%，TTFT降低2.0%-12.6%（性能增益在小缓存时更明显）。
  - 真实对话：命中率提升1.1%-2.1%，TTFT降低1.0%-2.0%。
- **自适应分区至关重要**：对比固定分裂的DBL，ARC在对话数据集的大缓存下（200K/300K tokens）表现更优，说明ARC能捕捉长期的工作负载偏移。
- **批量推理中优势保持**：随着batch size增大，ARC相对LRU的增益略有增加，命中率稳定。
- **动态可视性**：T1比例随时间变化图（图7）展示了ARC在不同负载下如何自动调整近期/频繁偏好，直观且可解释。

## 7. 优点

- **方法简洁但有效**：ARC是经典缓存算法，集成到vLLM的架构改动小，且参数无关（不需手动调优）。
- **可解释性强**：双队列大小动态变化过程可以可视化，有利于理解和调试缓存行为。
- **广泛适用性**：在不同类型工作负载（问答、对话）和不同缓存容量下均得到正向收益，并可扩展到批量推理。
- **开源实现**：代码已公开（GitHub），便于复现和进一步研究。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了Qwen3-14B模型，未在更大参数量（如70B/100B）或不同架构（如Mamba）上验证。
- **仅单GPU离线场景**：未评估在线推理、多GPU分布式、预填充-解码分离、层级CPU/磁盘卸载等真实生产环境。
- **数据集代表性**：合成文档QA可能无法完全反映真实文档访问分布；真实对话Trace虽来自大云厂商，但仅单个快照，可能缺乏普遍性。
- **未与最新学习型策略比较**：如基于学习的预测驱逐策略（Yang et al. 2025b）、或RAGCache（针对检索增强生成）等。
- **TTFT指标单独测量**：生成阶段固定为单token，忽略解码阶段长序列对吞吐和延迟的影响，可能低估实际性能差异。
- **硬件资源**：单GPU下缓存在速度和容量上有局限，结论向多GPU或CPU-offload推广时需谨慎。

（完）
