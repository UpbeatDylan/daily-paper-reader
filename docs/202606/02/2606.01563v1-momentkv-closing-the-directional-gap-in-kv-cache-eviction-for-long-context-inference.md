---
title: "MomentKV: Closing the Directional Gap in KV Cache Eviction for Long-Context Inference"
title_zh: MomentKV：弥合长上下文推理中KV缓存淘汰的方向性差距
authors: "Yu Li, Binxu Li, Tian Lan"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01563v1"
tags: ["query:pic"]
score: 9.0
evidence: 面向长上下文LLM推理的KV缓存驱逐，解决方向不匹配问题
tldr: 长上下文推理中，KV缓存驱逐的现有方法仅最小化保留令牌的注意力质量损失，但忽略了驱逐令牌与保留令牌之间的方向失配，导致即使少量驱逐质量也会放大输出误差。MomentKV通过维护驱逐令牌的矩统计（计数、key均值、value均值及协方差），在驱逐时识别与累积摘要对齐的令牌，并在推理时用封闭形式一阶近似补偿驱逐注意力输出。在LongBench和RULER上，MomentKV在多种缓存预算下均优于所有基线，尤其在激进压缩时提升显著。该方法从几何方向角度揭示了现有策略的局限，提供了可行的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 754, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 765, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 509, \"height\": 1058, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01563v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 601, \"height\": 448, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1347, \"height\": 1004, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 647, \"height\": 253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 785, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 652, \"height\": 351, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1280, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 943, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 860, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01563v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1406, \"height\": 414, \"label\": \"Table\"}]"
motivation: 现有KV缓存驱逐方法仅关注保留令牌的注意力质量，但忽略驱逐令牌与保留令牌的方向失配，导致小量驱逐质量放大为显著输出误差。
method: MomentKV维护驱逐令牌的矩统计（计数、key均值、value均值及value-key协方差），利用它们识别与累积摘要对齐的令牌，推理时用封闭形式一阶近似补偿驱逐输出。
result: 在LongBench和RULER上使用LLaMA-3.1-8B-Instruct和Qwen3-4B-Instruct，MomentKV在所有缓存预算下超越现有方法，尤其在高压缩率时增益最大。
conclusion: MomentKV通过方向对齐和近似补偿，有效解决了KV缓存驱逐中的方向失配问题，为长上下文推理提供了更高效的缓存管理方案。
---

## 摘要
基于Transformer的语言模型的自回归解码依赖于KV缓存，其内存占用随序列长度线性增长，成为长上下文推理的主要瓶颈。KV缓存淘汰通过保留固定大小的键值对子集并丢弃其余部分来解决这一问题。我们发现输出质量下降的主要来源并非被淘汰token上的残余注意力权重（现有方法已最小化此问题），而是保留与淘汰token集之间的方向性失配。具体而言，实际被淘汰的token通常与保留的token接近正交。因此，即使少量被淘汰的注意力权重也可能对最终的方向分布产生过大影响，并放大为显著的输出误差。这揭示了现有策略的根本局限性。为解决这一问题，我们提出MomentKV，它在被淘汰token集上维护紧凑的小规模矩统计量，包括计数、键均值、值均值以及值-键协方差。在淘汰过程中，利用矩统计量识别已与累积摘要良好对齐并捕获的token，使被淘汰集保持几何规则性。在推理时，它们产生被淘汰注意力输出的闭式一阶近似，形成选择性淘汰与精确校正之间的相互增强循环。在基于LLaMA-3.1-8B-Instruct和Qwen3-4B-Instruct的LongBench和RULER评估中，MomentKV在每种缓存预算下均优于所有基线，并在激进压缩下取得最大增益。

## Abstract
Autoregressive decoding in Transformer-based language models relies on the KV cache, whose memory footprint grows linearly with sequence length and becomes the primary bottleneck for long-context inference. KV cache eviction addresses this by retaining a fixed-size subset of key-value pairs and discarding the rest. We identify that a primary source of output degradation is not the residual attention mass on evicted tokens, which existing methods already minimize, but a directional mismatch between the retained and evicted token sets. Specifically, the evicted tokens in practice are often near-orthogonal to the retained ones. Thus, even a small evicted mass could have an oversized impact on the resulting direction distribution and amplify into substantial output error. This reveals a fundamental limit in existing strategies. To address this, we propose MomentKV, which maintains compact, small-size moment statistics over the evicted token set, including a count, key mean, value mean, and value-key covariance. During eviction, the moment statistics is leveraged to identify tokens already well aligned with and captured by the accumulated summary, keeping the evicted set geometrically regular. During inference, they yield a closed-form first-order approximation of the evicted attention output, forming a mutually reinforcing loop between selective eviction and accurate correction. On LongBench and RULER with LLaMA-3.1-8B-Instruct and Qwen3-4B-Instruct, MomentKV outperforms all baselines at every cache budget, with the largest gains under aggressive compression.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：基于Transformer的语言模型在长上下文推理时，KV缓存的内存占用随序列长度线性增长，成为主要瓶颈。现有KV缓存淘汰（eviction）方法仅关注保留token的注意力质量（即最小化被驱逐token上的注意力权重总和），但忽略了**保留token集与被驱逐token集之间的方向性失配**。
- **关键发现**：实际中，被驱逐的token向量与保留token的向量几乎正交（余弦相似度接近0），导致即使极小的被驱逐注意力质量（如<0.08）也会因方向差异被放大为显著的输出误差（平均相对误差>20%）。现有方法仅通过重新归一化（renormalization）无法恢复这种正交方向的损失。
- **整体含义**：指出现有策略的根本局限——仅通过改进token选择（降低w_E）无法突破性能上限，必须恢复被驱逐token的方向信息。

### 论文提出的方法论

- **核心思想**：在被驱逐token集上维护紧凑的**矩统计量**（moment statistics），包括计数ne、key均值¯k、value均值¯v、value-key协方差矩阵˜S。这些统计量用于：
  1. **指导驱逐**：通过计算每个保留token的“矩残差”∥v_j - ¯v - (˜S k_j)/(n_e √d)∥，组合注意力权重形成驱逐分数α_j·∥r_j∥，优先驱逐已被矩统计良好预测的token，使被驱逐集保持几何规则性。
  2. **校正推理**：利用闭式一阶近似ˆf_E(q) = ¯v + (˜S q)/(n_e √d)估计被驱逐输出，并用Jensen不等式下界ˆZ_E = n_e·exp(q^T¯k/√d)近似被驱逐分区函数，最终用加权和ˆf = ŵ_R f_R + (1-ŵ_R) ˆf_E替代原始仅保留输出。
- **关键技术细节**：
  - 统计量更新：驱逐时按ne+=1, s_k+=k_j, s_v+=v_j, S+=v_j k_j^T进行秩一更新，存储量为O(d²)（每头约32 KB），独立于上下文长度。
  - 自调节机制：当中心化logits方差σ大时，Jensen下界偏小导致ŵ_E低估，自动抑制不可靠的校正；σ小时逼近真实值，形成“驱逐-校正”相互增强循环。
- **算法流程**（文字描述）：
  1. 每个解码步：计算新token的key/value并加入缓存。
  2. 若缓存超过预算L，执行驱逐循环：
     a. 从矩统计恢复¯k, ¯v, ˜S。
     b. 对每个保留token计算残差r_j和驱逐分数。
     c. 选择最低分数token，更新矩统计并从缓存移除。
  3. 推理时：使用保留集计算f_R和Z_R；用矩统计计算ˆf_E和ˆZ_E；最后加权输出ˆf。

### 实验设计

- **数据集/场景**：
  - **LongBench**（双语多任务基准）：涵盖单文档QA、多文档QA、摘要、少样本学习、合成任务、代码补全。
  - **RULER**：控制上下文长度的检索、变量追踪、多跳推理任务（16K上下文，L=128）。
- **模型**：LLaMA-3.1-8B-Instruct（128K上下文）和Qwen3-4B-Instruct-2507（256K上下文），均采用GQA（32查询头、8KV头）。
- **对比方法**：H2O、SnapKV、PyramidKV、Ada-KV（四种代表性驱逐方法）。MomentKV基于Ada-KV框架，采用SnapKV的预填充协议（32-token观察窗口，首token作为注意力anchor，相邻4个token合并评分，自适应头间预算分配）。
- **缓存预算**：L ∈ {128, 256, 512, 1024}。

### 资源与算力

- **GPU**：所有实验在**两块NVIDIA H200 GPU**上完成。
- **训练/推理时长**：文中未明确报告总训练/推理时长，仅提及每token延迟对比（MomentKV比SnapKV增加1.6ms，约19.5%；比全缓存低65%）。未说明消融实验、不同预算的全套运行时间。

### 实验数量与充分性

- **实验数量**：
  - 主实验（表1、图4）：2个模型 × 4个预算 × 6任务类别 → 共48组分数。
  - RULER实验（表2）：2个模型 × 1个预算 × 5方法 → 10组。
  - 消融实验（表3）：分离“矩指导驱逐（MI）”和“归一化校正推理（NC）”在3个预算下的贡献 → 3组（MI/NC/联合 vs 基线）。
  - 近似阶数分析（表4）：比较0阶（仅¯v）、1阶（本文）的精度与开销 → 3种设置。
  - 额外分析：图2方向误差可视化、图3每样本/逐层误差、附录中半精度数值稳定性验证（FP16 vs FP32差值<0.05）。
- **充分性评价**：
  - **充分**：覆盖了不同模型规模、多种预算、多种任务类型（检索密集型、局部依赖型），并进行了组件消融和近似精度对比。
  - **客观公平**：所有基线使用相同预填充协议和缓存管理框架（基于Ada-KV），并报告了全缓存参考值。实验设置详细，代码未开源但描述充分。
  - **不足**：仅在8B和4B模型上验证，未扩展到更大模型（如70B）或更长上下文（>128K）；未与其他非驱逐类压缩方法（如量化、低秩分解）对比；仅评估了英文/中文混合的LongBench，缺乏纯英文或纯中文的独立验证。

### 论文的主要结论与发现

1. **方向失配是主要误差来源**：驱逐导致的误差中，方向放大系数γ(θ)（由θ≈90°时可达1以上）比注意力质量w_E（通常<0.1）影响更大。
2. **MomentKV在所有预算下优于基线**：在LongBench上，L=128时比最强基线Ada-KV提升+1.35（LLaMA）和+0.95（Qwen），并保持94.1%的全缓存性能；在RULER上提升+3.4/+3.5。
3. **驱逐与校正相互增强**：矩指导驱逐缩小中心化logits方差σ，提高近似精度；而更精确的校正又改善后续驱逐决策，产生超线性增益（组件联合收益大于各自收益之和）。
4. **优势在检索密集型任务最显著**：单文档QA、合成任务、RULER等需精确检索的任务提升最大；摘要、代码等局部依赖任务提升较小。
5. **随预算增大，绝对增益缩小但恢复率提高**：从L=128的+1.35降至L=1024的+0.59，但恢复率从31.7%升至59.6%，表明大预算下驱逐集更均匀，近似更准。

### 优点

- **创新性**：首次系统阐述并量化了驱逐中方向失配的问题，突破了仅优化w_E的传统思路。
- **方法简洁高效**：仅需维护O(d²)的矩统计（约6-7%缓存开销），推理时额外延迟1.6ms，且不随上下文长度增长。
- **理论扎实**：给出了误差分解公式（w_E·γ(θ)）、一阶近似的误差上界O(σ²)、Jensen下界的自调节性质，从数学上说明了方法有效性。
- **实验全面**：覆盖不同模型、预算、任务类型，做了充分的消融和近似阶数分析，验证了各组件贡献。

### 不足与局限

- **实验覆盖有限**：
  - 仅测试了8B和4B模型，未在更大模型（如LLaMA-3.1-70B）或极长上下文（256K以上）上验证。
  - 未与其他主流压缩技术（如KIVI量化、PALu低秩分解）进行对比，缺少非驱逐类方法的比较。
  - 数据集局限于LongBench和RULER，可能未充分代表真实应用场景（如长文档摘要、对话生成等）。
- **偏差风险**：
  - 实验基于Ada-KV框架和SnapKV协议，超参数（观察窗口大小32、chunk size 4）可能对某些模型非最优，但未做敏感性分析。
  - 结果中“Full Cache”基线可能因长上下文注意力计算精度（如FlashAttention近似）而并非真实全精度上限。
- **应用限制**：
  - 额外存储（每头32 KB）虽小，但在超多头分组（如GQA的8 KV头）下累积约4-10 MB，对资源极端受限的移动端或边缘设备可能仍构成压力。
  - 驱逐过程需计算每个保留token的矩残差，复杂度O(L d²)，虽在GPU上微秒级，但在CPU或低端GPU上可能成为瓶颈。
  - 方法依赖于预填充阶段的观察窗口，窗口大小固定的假设可能不适用于流式连续输入（如需长期记忆的对话任务）。

（完）
