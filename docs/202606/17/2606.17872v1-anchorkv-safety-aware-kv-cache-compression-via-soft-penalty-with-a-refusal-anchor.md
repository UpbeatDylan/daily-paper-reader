---
title: "AnchorKV: Safety-Aware KV Cache Compression via Soft Penalty with a Refusal Anchor"
title_zh: "AnchorKV: 基于拒绝锚点的软惩罚实现安全感知的KV缓存压缩"
authors: "Ning Ni, Yingjie Lao"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17872v1"
tags: ["query:pic"]
score: 9.0
evidence: 安全感知的KV缓存压缩，直接解决KV缓存瓶颈
tldr: 大型语言模型在长上下文任务中面临KV缓存内存瓶颈。现有缓存压缩方法虽降低开销，但易受越狱攻击或损害安全对齐。本文提出AnchorKV，一种即插即用的安全感知KV缓存压缩方案。通过构建离线安全锚点，在键空间中偏置令牌保留评分远离有害提示方向。软惩罚令牌选择机制在轻微牺牲效用的同时显著提升安全对齐，且惩罚为零时退化为原始压缩器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17872v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1112, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1360, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1093, \"height\": 680, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1087, \"height\": 651, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17872v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1629, \"height\": 558, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩方法在减少内存使用时，要么无法防御越狱攻击，要么激进驱逐导致安全对齐退化。
method: AnchorKV利用差值均值表示工程构建离线安全锚点，在层特定键投影空间上偏置令牌选择评分，远离有害方向。
result: 软惩罚规则在保持较小效用损失下大幅提升安全对齐；惩罚为零时等同于原压缩器，实现即插即用。
conclusion: 本文提出一种安全感知的KV缓存压缩方法，有效平衡效用与安全，可轻松集成到现有压缩器中。
---

## 摘要
大型语言模型（LLMs）在生成式推理和长上下文任务上优于早期架构，但其庞大的规模带来了内存使用、能耗和边缘部署方面的重大挑战。由于扩展预训练语言模型能提升下游能力\cite{zhao2023survey}，键值（KV）缓存成为主要的推理瓶颈。最近的KV缓存压缩方法\cite{jo2025fastkv,li2024snapkv,zhou2024dynamickv}通过仅保留注意力相关令牌的子集来降低这一成本。然而，尽管这些方法在良性工作负载下保留了准确性，其压缩策略要么无法防御越狱攻击\cite{jiang2024robustkv}，要么在激进淘汰下削弱了安全对齐。我们提出AnchorKV，这是一种对KV缓存压缩的即插即用修改，它使令牌保留分数偏离与有害提示相关的关键空间方向。AnchorKV通过将均值差异表征工程方法\cite{arditi2024refusal,zou2023representation}适配到KV缓存中使用的层特定键投影空间，构建了一个离线安全锚点。基于该锚点，一种软惩罚令牌选择规则以少量效用换取大幅提升的安全对齐，而当惩罚为零时则退化为原始压缩器。

## Abstract
Large language models (LLMs) outperform earlier architectures on generative inference and long-context tasks, but their large size introduces significant challenges in memory usage, energy cost, and on-device deployment. Since scaling pre-trained language models improves downstream capability \cite{zhao2023survey}, the key-value (KV) cache becomes a dominant inference bottleneck. Recent KV cache compression methods \cite{jo2025fastkv,li2024snapkv,zhou2024dynamickv} reduce this cost by retaining only a subset of attention-relevant tokens. However, while these approaches preserve accuracy on benign workloads, their compression policies either fail to defend against jailbreak attacks \cite{jiang2024robustkv} or degrade safety alignment under aggressive eviction.   We propose AnchorKV, a drop-in modification to KV cache compression that biases token retention scores away from directions in key space associated with harmful prompts. AnchorKV constructs an offline safety anchor by adapting a difference-of-means representation engineering approach \cite{arditi2024refusal,zou2023representation} to the layer-specific key projection space used in KV caching. Based on this anchor, a soft penalty token selection rule trades a small amount of utility for substantially improved safety alignment, while reducing to the original compressor when the penalty is zero.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLMs）的长上下文推理中，KV（键值）缓存的内存和延迟成为主导瓶颈。现有KV缓存压缩方法（如SnapKV、FastKV等）通过仅保留注意力相关的令牌子集来降低成本，在标准准确性基准上表现良好，但**完全忽略了与模型安全对齐的交互**。
- **核心问题**：论文发现两个现象：
    1. **压缩本身削弱防御**：以FastKV在25%保留率下，AdvPrompter越狱攻击成功率（ASR）从0.440升至0.506（自攻击），甚至从0.299升至0.632（跨模型转移攻击）。
    2. **压缩引发两种病理**：①“拒绝证据稀释”——有害查询令牌因高注意力而被淘汰，导致模型不再触发拒绝；②“模板模式回归”——内容令牌被淘汰后，模型输出无关的通用安全模板。
- **整体含义**：现有的准确性导向压缩设计无法保障安全，需要一种**安全感知的KV缓存压缩**方法，在保持高效推理的同时抵御越狱攻击，且不严重损害效用。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于表征工程（Representation Engineering）中“拒绝由单一线性方向中介”的结论，将该方向从残差流迁移到KV缓存压缩所操作的**层特定键投影空间**，从而在令牌保留评分中注入安全偏置。

- **关键技术步骤**：
    1. **离线锚点构建**：
        - 使用有害提示集（AdvBench训练集）和长度匹配的无害提示集（由DeepSeek-V3反意图改写生成）。
        - 在选定层ℓ*（实验中为第15层）上，对每个提示计算所有令牌键的平均向量ϕ(p)，然后计算两类的均值差得到锚点向量 v_h。
        - 校准阈值 θ = (median(harmful) + median(harmless))/2，解决投影偏置问题。
    2. **在线软惩罚选择**：
        - 对每个提示，计算各令牌的原始危害投影 h_t = 1/H Σ ⟨K_(ℓ*,h)_t, v_h∥v_h∥⟩。
        - 应用偏移产生非负危害信号：h⁺_t = max(h_t − θ, 0)。
        - 加入两层结构防护：① sink保护（前4个令牌豁免）；② 重要性免疫（保留注意力分数最高的α·(L−w)个令牌）。
        - 最终评分：score_t = a_t − λ · ā · fh⁺_t，其中λ为惩罚强度，ā为平均注意力分数，fh⁺_t为经防护后的危害强度。
        - 按score_t进行Top-K选择，形成保留集。

- **算法流程（文字说明）**：分为离线阶段（Algorithm 1）和在线阶段（Algorithm 2）。离线阶段：遍历候选层，计算锚点并选择AUROC最高层；计算阈值。在线阶段：若λ≤0则直接返回FastKV基线；否则计算危害投影，应用防护，生成带惩罚的评分，执行Top-K选择。

### 3. 实验设计：数据集、基准、对比方法

- **安全评估**：
    - **数据集**：AdvBench的60/20/20分割（训练312、验证104、测试104条有害提示），无害提示由DeepSeek-V3意图反转改写。
    - **攻击方法**：AdvPrompter训练的攻击者（使用Mistral-NeMo-Instruct-2407作为攻击模型进行转移攻击）。
    - **度量**：攻击成功率（ASR），通过关键词匹配判断拒绝。
- **效用评估**：
    - **基准**：LongBench（16个长上下文子任务，如NarrativeQA、Qasper、HotpotQA等），官方评估脚本。
    - **度量**：各子任务分数及未加权平均。
- **对比方法**：
    - FullKV（无压缩，效用上界）。
    - FastKV（未修改的骨干压缩器，保留率ρ=0.25）。
    - AnchorKV（本方法，在多个λ值下评估：0,0.3,1,3,10,30,100,300）。
- **锚点验证**：在验证集上计算AUROC和Cohen's d，在测试集上最终评估AUROC。

### 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长等算力资源**。仅提及目标模型为Llama-3.1-8B-Instruct（8B参数），攻击模型为Mistral-NeMo-Instruct-2407，锚点构建使用DeepSeek-V3生成无害提示。实验的复现所需算力取决于这些模型的推理开销，但未提供具体数据。

### 5. 实验数量与充分性

- **实验数量**：
    - 安全实验：在AdvBench测试集上对8个λ值测量ASR，每个λ下使用10个随机种子（Figure 1显示10个种子的均值和标准差）。
    - 效用实验：在LongBench全部16个任务上对8个λ值测量分数，提供完整表格（Table 1）。
    - 锚点验证：8个候选层的AUROC和Cohen's d（Figure 2），最终测试集AUROC（Figure 3）。
- **充分性**：
    - 实验覆盖了安全与效用两个关键维度，并提供了连续λ的扫描，展示了从有效区间到反转区间的完整趋势。
    - 通过λ=0的位精确验证，确保了与FastKV的无偏比较。
    - **不足之处**：没有直接与其他基于KV缓存的安全防御方法（如RobustKV）进行对比；仅使用一个目标模型（Llama-3.1-8B-Instruct），缺乏跨模型泛化验证；安全评估仅针对AdvPrompter攻击，未测试GCG、AutoDAN等其他越狱家族。
- **公平性**：实验设计合理，λ=0退化为FastKV基线，确保了比较的公正性；随机种子取均值，结果稳健。

### 6. 论文的主要结论与发现

1. **KV压缩会放大越狱脆弱性**：实验首次量化表明，压缩本身可导致ASR显著上升（如跨模型攻击中ASR从0.299升至0.632）。
2. **锚点在键空间具有高判别力**：在层15上的AUROC达到0.996，表明线性危害方向在键投影空间中存在且可恢复。
3. **AnchorKV有效降低ASR且效用损失小**：
    - 在λ∈[1,30]的宽范围内，ASR从0.68降至0.13‑0.17（相对降低高达81%）；λ=10时ASR最低（0.13）。
    - LongBench平均分从FastKV的47.64降至47.55‑47.21（λ≤3），损失小于压缩本身带来的1.59分降幅。
4. **存在非单调反转特性**：λ≥100时ASR反弹，λ=300时ASR达0.78，超过FastKV基线。这归因于过度惩罚清除了内容骨架令牌，导致模型退化为模板输出。
5. **安全与效用呈连续权衡**：λ从0到100的变化平滑，无突变，便于实际调参。

### 7. 优点

- **即插即用**：AnchorKV只需修改得分规则，可轻松集成到FastKV等基于注意力的压缩器中，且λ=0时退化为原始版本。
- **安全性提升显著**：在达到最低ASR时，效用损失（约1分）小于压缩本身带来的损失。
- **信号独立于注意力排名**：基于键空间几何，避免了注意力排名可被对抗操纵的弊端（如RobustKV）。
- **结构防护设计合理**：sink保护避免注意力汇令牌被错误淘汰；重要性免疫防止过度惩罚破坏内容骨架。
- **实验设计严谨**：连续λ扫描、位精确验证、10个随机种子，结果可靠。

### 8. 不足与局限

- **锚点表达能力有限**：仅使用单一线性方向，可能无法捕捉复杂或非线性的危害模式。
- **分布外泛化未验证**：锚点基于AdvBench构建并测试，未在JailbreakBench、HarmBench等其他越狱基准上评估。
- **缺乏与其他防御的直接比较**：未与RobustKV等现有防御方法进行性能、开销或兼容性对比。
- **仅用一个模型验证**：实验全部基于Llama-3.1-8B-Instruct，未展示在更大模型（如70B）或不同架构（如Mistral、DeepSeek）上的行为。
- **算力资源未报告**：缺少GPU规格、训练/推理时间等信息，影响可复现性评价。
- **攻击类型单一**：仅使用了AdvPrompter，未测试GCG、AutoDAN、多轮攻击等变体。

（完）
