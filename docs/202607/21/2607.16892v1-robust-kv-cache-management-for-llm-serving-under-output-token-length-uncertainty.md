---
title: Robust KV Cache Management for LLM Serving under Output Token Length Uncertainty
title_zh: 输出token长度不确定性下LLM服务的鲁棒KV缓存管理
authors: "Jiaming Cheng, Duong The Do, Duong Tung Nguyen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16892v1"
tags: ["query:pic"]
score: 9.0
evidence: 鲁棒KV缓存管理，在输出长度不确定下联合优化预留和前缀缓存
tldr: "LLM服务中KV缓存管理面临输出长度不确定导致的预留与抢占权衡问题。本文提出鲁棒缓存管理框架，联合优化GPU并行配置、缓存预留、请求路由和前辍缓存，采用Wasserstein分布鲁棒优化与块坐标下降算法解决。在BurstGPT等生产负载上，成本降低56%，同时保持延迟和SLO达标。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16892v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 1121, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 920, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 709, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16892v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 227, \"label\": \"Table\"}]"
motivation: 输出长度不确定性导致KV缓存预留难以平衡：预留不足引发高开销的抢占，预留过多浪费内存降低吞吐。
method: 建立Wasserstein分布鲁棒优化模型，结合块坐标下降算法求解混合整数规划，自动适配预留分位数。
result: "在BurstGPT、Azure、ShareGPT等生产负载上，相比固定分位数基线，成本降低达56%，P99延迟、吞吐和SLO违规率均具竞争力。"
conclusion: 提出的鲁棒KV缓存管理框架能有效应对输出长度不确定性，实现内存效率与低抢占风险的平衡。
---

## 摘要
KV缓存内存是现代在GPU集群上部署的LLM服务系统中的主要瓶颈。一个基本挑战是，KV缓存必须在请求到达时预留，而输出token长度直到生成完成才可知。预留不足会触发抢占——强制终止并重新计算请求，导致巨大开销——而预留过多则浪费内存并降低吞吐量。这就在内存效率和抢占风险之间产生了一个核心权衡。我们提出了一个用于LLM服务的鲁棒KV缓存管理框架，它联合优化了GPU并行配置、每请求类别的KV缓存预留、跨异构服务组的请求路由以及共享提示的前缀缓存。该框架包含延迟SLO约束，并捕捉了内存分配、吞吐量和排队延迟之间的相互作用。为了解决输出token长度不确定性和工作负载分布变化，我们开发了一个Wasserstein分布鲁棒优化（DRO）公式，以及针对所得混合整数问题的可扩展块坐标下降算法。我们的分析揭示了一个关键的分位数结构，它可以自动适应不同的抢占和内存成本机制，无需手动调整。在生产LLM工作负载（包括BurstGPT、Azure和ShareGPT跟踪）上的跟踪驱动评估表明，与固定分位数预留基线相比，成本降低高达56%，同时在各种操作条件下保持有竞争力的P99延迟、有效吞吐量和SLO违规率。

## Abstract
KV cache memory is a primary bottleneck in modern LLM serving systems deployed on GPU clusters. A fundamental challenge is that the KV cache must be reserved upon request arrival, while the output token length remains unknown until generation completes. Under-reservation triggers preemption -- forcing termination and recomputation of requests and incurring significant overhead -- whereas over-reservation wastes memory and reduces throughput. This creates a central trade-off between memory efficiency and preemption risk. We present a robust KV cache management framework for LLM serving that jointly optimizes GPU parallelism configuration, KV cache reservation per request class, request routing across heterogeneous serving groups, and prefix caching for shared prompts. The framework incorporates latency SLO constraints and captures the interaction between memory allocation, throughput, and queueing delay. To address output token length uncertainty and workload distribution shift, we develop a Wasserstein distributionally robust optimization (DRO) formulation together with a scalable block coordinate descent algorithm for the resulting mixed-integer problem. Our analysis reveals a critical fractile structure that automatically adapts reservation quantiles to different preemption and memory cost regimes without manual tuning. Trace-driven evaluation on production LLM workloads, including BurstGPT, Azure, and ShareGPT traces, demonstrates up to 56\% lower cost than fixed-quantile reservation baselines while maintaining competitive P99 latency, goodput, and SLO violation rates across diverse operating regimes.

---

## 论文详细总结（自动生成）

# 输出 Token 长度不确定性下 LLM 服务的鲁棒 KV 缓存管理 —— 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心瓶颈**：LLM 推理时，KV 缓存内存随生成 token 线性增长，但必须在请求到达时预先分配，而输出 token 长度直到生成完成才已知。
- **根本矛盾**：预留不足 → 触发抢占（preemption）：丢弃已生成 token 并重新计算，开销巨大；预留过多 → 浪费内存，降低有效并发数（goodput），增加排队延迟。
- **三个核心挑战**：
  - **工作负载异质性**：不同应用类别（聊天、代码生成、QA）输出长度差异巨大，单一全局分位数无法同时最优。
  - **非平稳性**：用户行为、提示模板随时间变化，基于历史数据拟合的预留策略在分布偏移时性能下降。
  - **系统异构性**：现代部署将 GPU 划分为不同并行配置组（TP/PP），每组拥有不同的 KV 容量、吞吐和延迟特性，需要联合决策并行配置、预留、路由、前缀缓存等。
- **研究空白**：现有工作（PagedAttention、Orca、DistServe、Sarathi-Serve 等）主要关注运行时执行效率，但将预留与路由决策留给操作员手动设定；固定分位数启发式（Mean、P90、P95、P99、Max）无法自适应成本结构变化；分布鲁棒优化（DRO）尚未针对 LLM KV 缓存问题建模。因此，论文提出两个研究问题：**RQ1** 如何在输出长度不确定性下预留 KV 缓存以最小化抢占与浪费的总成本？**RQ2** 最优预留策略是什么，如何无需手动调参自动适应不同成本结构？

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
- 建立 **分布鲁棒优化（DRO）** 框架，将预留决策建模为经典报童问题（newsvendor problem）的扩展，但扩展到耦合内存容量约束、并行配置、路由、前缀缓存的联合优化。
- 利用 **Wasserstein 距离** 构建 ambiguity set，抵御分布偏移，同时推导出**关键分位数（critical fractile）**结构：最优预留量 \( r_i^* = L_i^{in} + F_i^{-1}(\rho/(\rho+1)) \)，其中 \(\rho = c_p/c_w\) 是抢占成本与浪费成本之比，分位数自动随成本比变化，无需手动调整。
- 设计 **块坐标下降算法（BCD-DRO）**，将原混合整数双线性规划（MIBLP）分解为连续子问题（线性规划）和整数子问题（闭式更新 + 枚举），实现生产规模下可扩展求解。

### 2.2 关键技术细节
- **系统模型**：
  - 集群含 \(J\) 块 GPU，划分成若干服务组，每组使用一种并行配置 \(k \in \mathcal{K}\)（TP 度 \(\tau_k\)、PP 度 \(p_k\)），每类请求 \(i \in \mathcal{I}\) 有到达率 \(\lambda_i\)、输入长度 \(L_i^{in}\)、共享前缀长度 \(L_i^{pre}\)、延迟 SLO \(D_i\)、输出长度不确定 \(\xi_i\)。
  - 服务时间公式（1）：\(T_{i,k} = p_k \alpha L_i^{in}/C_k + \beta \mathbb{E}[\xi_i]/B_k + N_L \tau_k t_{ar}\)。
- **决策变量**：
  - \(z_k\)：配置 \(k\) 的服务组数（整数）
  - \(r_i\)：第 \(i\) 类请求预留的 KV 缓存 token 数（连续）
  - \(\pi_{i,k}\)：路由比例（连续）
  - \(\delta_{i,k} \in \{0,1\}\)：是否缓存共享前缀
- **成本结构**：
  - 抢占成本：\(c_p (L_i^{in} + \xi_i - r_i)^+\)
  - 浪费成本：\(c_w (r_i - L_i^{in} - \xi_i)^+\)
  - 另有 GPU 使用成本、SLO 违规惩罚、拒绝成本
- **Wasserstein DRO 公式**（式 6）：
  - 对每个类别，构造 ambiguity set \(\mathcal{P}_i^\varepsilon = \{P: W_1(P,\hat{P}_N) \leq \varepsilon\}\)，并在目标中对 worst-case 期望求 inf。
- **可解蜕化**（Proposition IV.1）：
  - 利用 Kantorovich 对偶，将 DRO 转化为有限维线性规划（增加辅助变量 \(\gamma_i, \theta_{i,n}\)），约束 (8c)-(8f) 编码支持边界处的概率质量放置。
- **关键分位数结构证明**（Proposition IV.2）：
  - 对于固定路由下的预留子问题，通过对期望成本求导，得到最优缓冲 \(\eta^* = F_i^{-1}(\rho/(\rho+1))\)，其中 \(\rho = c_p/c_w\)。DRO 下 worst-case 分布保持该分位数结构。
- **BCD-DRO 算法**（Algorithm 1）：
  - 初始化：\(z^{(0)},\delta^{(0)}\leftarrow 0,\gamma_i^{(0)}\leftarrow c_p, r_i^{(0)}\leftarrow L_i^{in}+\mathbb{E}[\xi_i]\)
  - 迭代三个块：
    1. 交替求解两个线性规划（固定 \(\gamma,\theta,r\) 优化 \(\pi\)；固定 \(\pi\) 优化 \(r,\gamma,\theta\)）
    2. 闭式更新前缀缓存 \(\delta_{i,k}^{(t)} = \mathbb{1}[u_{i,k}\pi_{i,k}/z_k > 1]\)
    3. 枚举有限配置集合 \(\mathcal{Z}\) 选择最优 \(z^{(t)}\)
  - 收敛：目标单调下降，有限步内达到块最优（Proposition IV.3）

## 3. 实验设计

### 3.1 数据集 / 场景
- **BurstGPT**：Azure OpenAI 1.4M 请求，61 天，输出长度均值 125、P90=276、P99=1586 tokens。
- **Azure LLM 2024**：Azure 生产 44M 请求，7 天，包含 Code（均值 23）和 Conversation（均值 117）两类。
- **ShareGPT**：368K 众包 ChatGPT 会话，输出均值 265、P90=486、P99=768 tokens。

### 3.2 评价场景
- 根据成本比 \(\rho\) 划分为 6 个场景：Batch（\(\rho=2\)）、Async API（5）、User-facing（10）、Latency-sensitive（20）、Critical（50）、Real-time（100），覆盖从批处理到实时系统的不同抢占容忍度。

### 3.3 对比方法
- **固定分位数基线**：Max、Mean、P90、P95、P99。
- **长度预测基线**：LP+1σ、LP+2σ（使用历史均值+标准差的线性预测）。
- **消融变体**：−Routing（均匀路由）、−DRO（SAA，\(\varepsilon=0\)）、−Prefix caching（\(\delta=0\)）。

## 4. 资源与算力

- 模拟配置：**8 块 A100-80GB GPU**，服务 70B 参数模型。
- 候选并行配置：k1 (TP=2, PP=1)、k2 (TP=4, PP=1)、k3 (TP=2, PP=2)、k4 (TP=4, PP=2)。
- 文中没有报告实际训练/求解的 GPU 小时数（因为实验是仿真驱动，BC-DRO 算法在 CPU 上求解优化问题），仅给出了求解时间：在最大规模测试（\(|\mathcal{I}|=15, |\mathcal{K}|=12, N=2000\)）下 BCD-DRO 收敛约 257.7 秒，适合 5–10 分钟重优化周期。实际运行时一个请求仅需 \(O(1)\) 查表。

## 5. 实验数量与充分性

- **主要实验**：
  - 成本 vs 成本比 \(\rho\)（图 2a，覆盖 \(\rho=1\) 到 100）
  - DRO 分位数 vs 理论值 \(q^*=\rho/(\rho+1)\)（图 2b）
  - 算法可扩展性（表 III）：小规模（3×4×100）到大规模（15×12×2000），比较 MIBLP 与 BCD-DRO 求解时间。
  - 敏感性分析：到达率缩放（图 3a）、Wasserstein 半径 \(\varepsilon\)（图 3b）、其他成本权重 \(c_{slo}, c_{rej}, \kappa\)（图 4）。
  - 分布偏移鲁棒性（图 5）：静态 vs 滚动时域、不同偏移程度和 \(\rho\) 下的 DRO 优势。
  - 成本与延迟权衡（表 IV）：6 种方法在 3 种成本比下的成本和在 \(\rho=10\) 下的 P99 延迟与 SLO 违规率。
  - 消融实验（表 V）：在异质实例（\(\rho=20\)，2.5× 偏移）下对比完整 BCD-DRO 与三个消融变体，报告成本、P99、有效吞吐、SLO 违规率，并给出均值±标准差（5 种子）。
  - DRO vs SAA 随偏移因子变化（图 6）：偏移 1.0× 到 3.0×。
- **充分性评估**：实验覆盖了 3 个真实生产跟踪、6 个成本比场景、多个基线（6 种固定分位数 + 2 种长度预测 + 3 种消融）、参数敏感性（\(\rho, \varepsilon, c_{slo}, c_{rej}, \kappa\)）、分布偏移（幅度 0%–80%）、算法可扩展性（从小到大规模）。实验设计比较全面，对比基线包括当前主流系统使用的默认策略（P90 对应 DistServe, P95 对应 vLLM, Mean 对应 Orca），公平性较好，但缺少与更先进的学习型预测方法的对比（论文提及了但未详述）。随机种子报告了均值和标准差（5 次），确保了统计可靠性。

## 6. 主要结论与发现

1. **关键分位数消除手动调参**：最优预留分位数 \(q^* = \rho/(\rho+1)\)，DRO 自动匹配该值，而固定分位数基线只在特定 \(\rho\) 下最优。例如 P90 在 \(\rho=10\) 附近最优，P95 在 \(\rho=20\) 附近最优，跨成本比时优势丧失，成本可能差数倍。
2. **DRO 成本优势**：相比 P90 节省最高 56%（Real-time 场景），相比 P95 节省最高 44%，且在所有 \(\rho\) 下成本始终处于低位；固定分位数在某些 \(\rho\) 下成本激增（如 Mean 在 \(\rho=100\) 成本高达 42,811，远高于 DRO 的 4,807）。
3. **鲁棒性**：Wasserstein DRO 相比于 SAA（\(\varepsilon=0\)）在分布偏移时 P99 延迟增长更慢（偏移 3.0× 时 DRO P99=9.1s vs SAA 18.0s），SLO 违规率低约 30%。滚动时域重新优化进一步适应工作负载漂移。
4. **算法可扩展性**：BCD-DRO 可在数分钟内求解含 15 个请求类、12 种配置、2000 个样本的实例，而直接 MIBLP 求解器在 6 类、6 配置、300 样本时已超时。
5. **路由关键性**：均匀路由（−Routing）导致成本暴涨近 7 倍、P99 延迟约 178s、有效吞吐仅 0.63%，显示联合路由优化的必要性。
6. **前缀缓存和鲁棒半径**：前缀缓存主要降低排队延迟（特别是高并发时）；Wasserstein 半径 \(\varepsilon\) 在 0.1–0.2 区间时额外开销 <0.5%，提供有效风险对冲。

## 7. 优点

- **理论深入**：给出了 DRO 的可解线性规划等价形式（Proposition IV.1），并证明了最优预留的关键分位数结构（Proposition IV.2），将高度实践的问题与经典库存理论连接，具有解释性和通用性。
- **联合优化范围广**：首次在一个框架内同时优化并行配置、预留、路由、前缀缓存四个控制变量，而之前的工作仅关注其中一二。
- **鲁棒性保证**：Wasserstein DRO 提供对抗分布偏移的形式化保护，且推导了闭合形式，无需黑盒方法。
- **实用算法**：BCD-DRO 采用块坐标下降，将难题分解为 LP + 枚举，可扩展到生产规模（数十类、数千样本），支持在线周期性重优化。
- **消融充分**：通过三项消融（去除路由、去除 DRO、去除前缀缓存）清晰展示了各组件的贡献。
- **实验范围全面**：涵盖三个真实工作负载、六个成本比场景、分布偏移对抗、参数敏感性，并给出了统计波动（标准差）。

## 8. 不足与局限

- **建模局限**：
  - 排队模型采用 M/G/1 假设（Poisson 到达、一般服务时间），在高度突发或自相似的 LLM 请求模式下可能存在偏差（文中未讨论更复杂的流量模型）。
  - 服务时间公式假设预填充阶段计算有界、解码阶段带宽有界，忽略了对高负载时的指令级或批处理效应，可能过于简化。
  - 内存容量约束使用 Little 定律计算平均并发数，添加固定安全因子 \(\kappa\)，但未建模瞬时内存峰值导致的极端抢占。
- **实验局限**：
  - 实验仅基于仿真（非真实集群部署），虽然使用真实跟踪驱动，但未在真实 runtime（如 vLLM）上验证端到端性能差距。
  - 对比的固定分位数基线是简化版（按全局分位数预留），实际系统（如 vLLM 的 P95 可能结合内存对齐）可能略有不同，但论文声称对齐。
  - 缺少与更先进的预测式方法（如基于 prompt embedding 预测输出长度）的对比，虽然 LP+1σ 和 LP+2σ 用作代表。
- **资源假设**：所有实验固定 8 块 A100 GPU，未讨论更大规模集群（数百 GPU）时的可扩展性，只能通过算法复杂度推断。
- **敏感性参数**：关键参数 \(\rho\) 和 \(\varepsilon\) 仍需操作员指定，论文虽给出了自适应分位数，但 \(\rho\) 本身需根据业务目标（如抢占 vs 成本容忍度）设定，未给出自动确定 \(\rho\) 的方法。
- **响应时间建模**：Wasserstein 半径 \(\varepsilon\) 的设置遵循文献经验值（0.15·mean），未提供针对特定工作负载的自适应确定性方法。
- **异质性覆盖**：仅考虑了 TP/PP 异构，未涉及预填充/解码分离（disaggregation）或 CPU offloading 等更复杂拓扑。

（完）
