---
title: "DepthWeave-KV: Token-Adaptive Cross-Layer Residual Factorization for Long-Context KV Cache Compression"
title_zh: DepthWeave-KV：面向长上下文KV缓存压缩的令牌自适应跨层残差分解
authors: "Anna Cordoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesus Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06523v1"
tags: ["query:pic"]
score: 9.0
evidence: 面向长上下文的令牌自适应KV缓存压缩
tldr: 长上下文语言模型推理受限于KV缓存带宽，现有压缩方法因统一预算导致检索退化。本文提出DepthWeave-KV，通过跨层残差分解和token自适应路由实现8.3倍内存压缩，在LongBench等基准上接近全缓存质量，解码速度达72.8 token/s (64K上下文)。该方法无需重训模型，在线自适应压缩。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06523v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06523v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06523v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 909, \"height\": 545, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06523v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1115, \"height\": 1033, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩方法在长上下文中统一压缩预算，忽视token重要性差异，导致检索任务质量下降。
method: 跨层共享低秩基分解键值状态，保留token特定残差；基于注意力输出探针的在线误差追踪，为关键token分配更高重建秩。
result: 在LongBench等基准中平均得分优于先前方法，8.3倍内存缩减，64K上下文下72.8 token/s解码速度，接近全缓存性能。
conclusion: DepthWeave-KV实现了token自适应的高效KV压缩，兼顾内存节省与任务质量，适用于长上下文推理。
---

## 摘要
长上下文语言模型推理日益受到存储键值缓存所需内存带宽和容量的限制，然而现有压缩方法通常在各层或令牌上应用统一预算，并且在需要不同保留程度的词汇线索和语义状态时，检索性能会下降。我们提出DepthWeave-KV，一种令牌自适应缓存压缩方法，它使用共享的低秩通道基对相邻Transformer层中的键值状态进行分解，同时在注意力行为敏感的令牌处保留轻量级的令牌特定残差。DepthWeave-KV结合跨深度残差分解与令牌条件深度路由器，为指令承载和检索关键的令牌分配更高的重建秩，并使用来自注意力输出探针的免校准在线误差跟踪，在生成过程中自适应压缩而无需重新训练基础模型。融合的CUDA实现联合执行基查找、残差反量化和注意力投影，以减少解码时的内存流量。在LongBench、Needle-in-a-Haystack、L-Eval以及长格式问答和摘要基准测试中，DepthWeave-KV以显著更低的内存使用达到接近全缓存的任务质量，在先前压缩缓存的基础上提高了平均得分和检索准确性，同时在64K上下文下实现了8.3倍的KV内存缩减和每秒72.8个令牌。

## Abstract
Long-context language model inference is increasingly limited by the memory bandwidth and capacity required to store key-value caches, yet existing compression methods often apply uniform budgets across layers or tokens and degrade retrieval when lexical cues and semantic states require different preservation. We introduce DepthWeave-KV, a token-adaptive cache compression method that factorizes key and value states across neighboring transformer layers using shared low-rank channel bases while retaining lightweight token-specific residuals where attention behavior is sensitive. DepthWeave-KV combines cross-depth residual factorization with a token-conditional depth router that allocates higher reconstruction rank to instruction-bearing and retrieval-critical tokens, and uses calibration-free online error tracking from attention-output probes to adapt compression during generation without retraining the base model. A fused CUDA implementation jointly performs basis lookup, residual dequantization, and attention projection to reduce decode-time memory traffic. Across LongBench, Needle-in-a-Haystack, L-Eval, and long-form QA and summarization benchmarks, DepthWeave-KV achieves near-full-cache task quality with substantially lower memory use, improving average score and retrieval accuracy over prior compressed caches while reaching 8.3x KV memory reduction and 72.8 tokens per second at 64K context.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文语言模型推理时，KV缓存占用了大量显存带宽和容量，成为主要瓶颈。现有压缩方法（如令牌逐出、量化、层共享等）通常对各层或各令牌施加**统一压缩预算**，忽视了不同令牌（如指令词、实体词、检索关键信息）和不同层（浅层词汇、深层语义）对缓存保真度的差异化需求，导致在长文本检索、多跳问答等任务中出现**检索退化**与**推理质量下降**。
- **整体意义**：需要一种同时考虑**深度依赖性**和**令牌自适应性**的压缩机制，在不重新训练基座模型的前提下，根据生成过程中的实际注意力误差动态调整压缩率，从而在显著降低内存的同时保持接近全缓存的任务性能。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：**跨层残差分解 + 令牌自适应路由 + 在线误差跟踪**。利用相邻Transformer层间KV缓存的冗余性，在深度窗口内共享低秩通道基，同时用轻量级残差针对性恢复仅对检索/指令敏感的信息。

- **技术细节**：
  - **跨深度残差分解**：将相邻层（窗口大小为w）内每个注意力头的键/值分解为共享基矩阵（\(B_{m,h}^K, B_{m,h}^V \in \mathbb{R}^{r_b \times d}\)）和层依赖的系数向量（\(A_{\ell,h,t}^Z\)）以及门控残差（\(g_{\ell,h,t}^Z R_{\ell,h,t}^Z\)）。残差仅在路由决定的秩上存储（4-bit量化），系数为8-bit块式量化。共享基在预填充阶段在线初始化，并随生成使用指数移动平均更新。
  - **令牌条件深度路由器**：根据令牌的累积注意力权重、最大注意力峰值、指令/分隔符标记、以及前一探测区间的注意力输出重构误差，计算综合得分并分配残差秩（三级：ρ=0/2/4/8）。路由器参数通过学习得到，决策仅基于元数据，无需额外前向传播。
  - **免校准在线误差跟踪**：每p个解码步，选取少量头部和查询位置，计算压缩版本与临时高保真版本之间的注意力输出相对误差（\(e_{\ell,h}\)）。若误差超过窗口容忍阈值，则降低阈值\(\tau_m\)并分配更多令牌到更高残差秩；反之则增加阈值以节省内存。该机制直接针对下一层隐藏状态的准确性，无需校准数据。
  - **融合CUDA实现**：解码时，融合核函数同时执行基查找、残差反量化、系数乘法和注意力打分，避免将重建的KV张量写回全局内存，显著减少内存流量。

### 3. 实验设计：数据集、基准、对比方法

- **数据集/任务**：LongBench、Needle-in-a-Haystack、L-Eval、NarrativeQA、Qasper、HotpotQA、MultiFieldQA-en、GovReport、QMSum、TriviaQA。覆盖检索、问答、摘要。
- **基准**：Full KV Cache（无压缩），压缩基线包括StreamingLLM、H2O、SnapKV、PyramidKV、MiniCache、KVSharer、ChunkKV、TailorKV、Eigen Attention。
- **配置**：基础模型、分词器、提示格式、解码参数在所有方法中相同。默认64K预填长度，贪婪解码512个生成令牌。压缩方法以至少4倍内存缩减为目标调整最强任务分数。

### 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长。仅提到实现了融合CUDA核，暗示在NVIDIA GPU上运行，但未提供具体计算资源消耗。可能的训练（路由器参数学习）仅在一次无标签长文本上使用冻结教师缓存目标，但未说明具体开销。

### 5. 实验数量与充分性

- **实验数量**：主实验在10个数据集上报告平均得分、检索准确率、内存缩减倍数、解码吞吐、TTFT、PPL增量、重建误差共7项指标。并进行了**4组消融实验**（去除跨深度基共享、去除令牌条件路由器、去除在线误差跟踪、去除残差门、仅共享基、去除融合核）。此外，按任务类别（提取/检索类 vs 摘要类）进行了分离分析。
- **充分性**：实验覆盖了主流长上下文任务，对比方法齐全（10种），消融设计合理，系统指标（吞吐、TTFT）与质量指标（PPL、重建误差）均报告，对比公平（相同基座模型与配置）。但未包含更细粒度的内存预算/步长扫描，且仅在一种上下文长度（64K）上进行主实验，未展示长度缩放行为。

### 6. 论文的主要结论与发现

- **主结果**：DepthWeave-KV在64K上下文下达到平均任务分数62.9%，与全缓存（63.8%）仅差0.9分，同时实现8.3倍KV内存缩减，解码吞吐72.8 tok/s（优于所有压缩基线）。Needle检索准确率96.1%，远高于TailorKV（92.6%）。PPL增量仅为0.09，重建误差0.027，均为最低。
- **消融关键**：令牌自适应路由对检索鲁棒性贡献最大（去除后Needle准确率降至92.4%）；跨深度基共享提供主要内存/吞吐增益；在线误差跟踪使平均得分提升1.2分，重建误差降低；共享基+无残差方案虽达10.2倍压缩但丢失4.0平均分；融合核提升吞吐15%。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 提出**跨层残差分解**，比直接层共享（如MiniCache）灵活，避免统一保真度；**令牌条件路由器**实现细粒度容量分配，不对低重要性令牌硬驱逐。
  - **在线注意力输出探针**直接监控对下一隐藏状态的影响，比单纯最小化KV向量误差更可靠，且无需校准数据。
  - **融合CUDA实现**压缩-计算协同设计，减少全局内存读写，对长上下文场景符合带宽瓶颈特征。
- **实验亮点**：涵盖多种任务类型（检索、QA、摘要）和系统指标；消融实验逐分量验证设计合理性；与10种基线对比，包括最新方法（ChunkKV、TailorKV等）。

### 8. 不足与局限

- **实验覆盖不足**：仅在固定上下文长度（64K）上进行主实验，未展示在更短（如8K）或更长（如128K/256K）长度下的性能缩放；未测试不同解码长度或批大小的影响。
- **模型泛化性**：仅在一种基座模型上验证，未说明对其他架构（如混合注意力、稀疏模型、MoE）的适用性；假设相邻层缓存冗余局部化，可能不适用于角色异质明显的模型。
- **资源信息披露缺失**：未提供训练路由器参数的实际算力需求、预填充阶段基更新开销、在线探测（每p步额外计算）的代价，难以评估实际部署成本。
- **潜在偏差风险**：路由器的特征仅基于4个在线特征，可能对罕见但重要的令牌（如表格单元格、代码标识符）识别不足；阈值调整策略可能对噪声敏感。
- **缺乏理论分析**：未给出压缩后注意力近似误差的上界，或对模型输出分布变化的理论保证。

（完）
