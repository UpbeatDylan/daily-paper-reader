---
title: "FreqDepthKV: Frequency-Guided Depth Sharing for Robust KV Cache Compression in Long-Context LLM Inference"
title_zh: "FreqDepthKV: 频率引导的深度共享用于长上下文LLM推理中的稳健KV缓存压缩"
authors: "Anna Córdoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesús Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06519v1"
tags: ["query:pic"]
score: 9.0
evidence: 通过深度共享的频率引导KV缓存压缩
tldr: 长上下文LLM推理受KV缓存内存和带宽限制，但激进压缩会丢失层特异性证据。本文提出FreqDepthKV，通过频率引导的深度共享，将相邻层状态分解为共享低频分量和稀疏高频残差，并在线为注意力头分配共享深度、残差深度或精确缓存模式。在32k上下文下，该模型在多项任务中接近全KV准确率（如Exact Match 58.3），解码吞吐70.4 tokens/s，峰值内存降至6.2GB，实现3.9倍有效压缩比。该方法无需重训练，自适应提示结构，兼顾效率与准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06519v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 671, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06519v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06519v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06519v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 427, \"label\": \"Table\"}]"
motivation: 长上下文推理中KV缓存开销巨大，现有压缩方法忽视层特异性信息，导致检索和推理性能下降。
method: 将相邻层KV状态分解为共享低频深度分量和稀疏高频残差，通过在线探测根据重建敏感logits贡献分配缓存模式。
result: "在32k预填窗口下，EM 58.3、F1 63.0、ROUGE-L 32.5、pass@1 48.1；解码吞吐70.4 tokens/s，TTFT 2.06s，峰值内存6.2GB，压缩比3.9x。"
conclusion: FreqDepthKV无需重训练即可自适应压缩，在保持精度的同时大幅降低内存和延迟，适用于长上下文LLM推理。
---

## 摘要
长上下文LLM推理日益受到KV缓存的内存和带宽成本限制，然而激进的压缩会移除检索和多步推理所需的层级特定证据。我们提出FreqDepthKV，一种推理时缓存压缩方法，将相邻层KV状态分解为共享的低频深度分量和稀疏的高频残差。一个轻量级的在线探针根据注意力头对重建敏感注意力logits的贡献，将其分配到共享深度、残差深度或精确缓存模式，使压缩策略能够适应提示结构而无需重新训练。在长上下文问答、针检索、摘要和代码生成基准测试中，FreqDepthKV在显著更小的缓存预算下保持了任务准确性。在32k令牌预填窗口下，FreqDepthKV达到了58.3的精确匹配、63.0的F1、32.5的ROUGE-L和48.1的pass@1，与完整KV相当，同时优于先前的压缩缓存方法。它还将解码吞吐量提升至70.4令牌/秒，将首个令牌生成时间（TTFT）降至2.06秒，并将峰值KV内存降至6.2 GB，实现了3.9倍的有效压缩比。

## Abstract
Long-context LLM inference is increasingly limited by the memory and bandwidth cost of KV caches, yet aggressive compression can remove the layer-specific evidence needed for retrieval and multi-step reasoning. We introduce FreqDepthKV, an inference-time cache compression method that factorizes adjacent-layer KV states into shared low-frequency depth components and sparse high-frequency residuals. A lightweight online probe assigns attention heads to shared-depth, residual-depth, or exact cache modes according to their contribution to reconstruction-sensitive attention logits, allowing the compression policy to adapt to prompt structure without retraining. Across long-context question answering, needle retrieval, summarization, and code generation benchmarks, FreqDepthKV preserves task accuracy under substantially smaller cache budgets. With a 32k-token prefill window, FreqDepthKV reaches 58.3 Exact Match, 63.0 F1, 32.5 ROUGE-L, and 48.1 pass@1, closely matching full KV while outperforming prior compressed-cache methods. It also improves decoding throughput to 70.4 tokens/s, reduces TTFT to 2.06 seconds, and lowers peak KV memory to 6.2 GB, achieving a 3.9x effective compression ratio.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

长上下文大语言模型（LLM）推理的主要瓶颈已从参数存储转移到键值（KV）缓存。在自回归解码过程中，每个生成令牌需要关注所有先前令牌的缓存，导致内存流量和缓存占用随上下文长度线性增长，尤其在长上下文问答、多文档摘要和代码补全等场景中，提示可能包含大量无关令牌，但仍需保留少量关键证据。现有缓存压缩方法（如令牌驱逐、量化、结构化共享）通常将冗余视为令牌层级或精度层级现象，但忽略了相邻Transformer层常编码相似的深度方向缓存结构。MiniCache等早期工作已证明沿深度维度共享缓存可大幅减少内存，但均匀深度压缩会抹除检索敏感和推理敏感提示中关键的高频残差信息，导致下游注意力logits偏移，改变生成答案。FreqDepthKV旨在解决这一“均匀深度共享丢失层特异性证据”的失败模式。

## 2. 论文提出的方法论

**核心思想**：将相邻层的KV缓存分解为两类分量：
- **低频深度分量**（共享）：在不同层间缓慢变化、可安全共享的部分。
- **高频残差**（稀疏保留）：对特定令牌-头-层交互至关重要的高频信息，用于保护检索敏感的证据。

**关键技术细节**：
- **深度-频率分解**：对每块连续B层（默认B=2或4），沿深度轴堆叠KV缓存，应用固定DCT正交变换分离低频和高频系数。低频系数作为共享深度分量存储一次，高频系数仅在选定的令牌-头对中保留残差。
- **在线路由探针**：预填阶段为每个注意力头评估三种缓存模式下的重构损失：
  - **共享深度模式**（shared-depth）：仅保留低频系数。
  - **残差深度模式**（residual-depth）：保留共享分量加稀疏高频率残差。
  - **精确模式**（exact）：保留完整原始缓存。
  损失函数比较原始与重构注意力logits的L2距离，并加入归一化内存成本惩罚项。每个头选择损失最小的模式。
- **稀疏残差选择**：对于残差深度头，根据移除高频系数后对探针查询位置产生的最大logit扰动评分，保留top-r比例令牌的高频残差。
- **内存预算控制**：通过调整损失中的惩罚系数λ，使估计峰值KV足迹满足目标压缩比（实验目标约3.8×）。
- **兼容性**：可与其他压缩方法（令牌驱逐、量化）串联使用，但实验中作为独立深度频率压缩方法评估。

**公式**：论文使用DCT变换 \( \mathbf{X}_{b,h} \) 到 \( \mathbf{Z}_{b,h} \)，重构为 \( \mathbf{F}_B^\top \mathbf{Z}_{b,h} \)，路由损失基于原始与重构注意力logits的差异与内存成本。

## 3. 实验设计

- **数据集/场景**：
  - 问答与检索：LongBench, Needle-in-a-Haystack, L-Eval, NarrativeQA, Qasper
  - 摘要：GovReport, MultiNews
  - 代码生成：HumanEval, MBPP
- **基准（benchmark）指标**：精确匹配（EM）、F1、ROUGE-L、pass@1，以及系统指标：解码吞吐量（tokens/s）、首个令牌生成时间（TTFT）、峰值KV内存、有效压缩比。
- **对比方法**：全量KV（Full KV），令牌驱逐类（StreamingLLM, H2O, Scissorhands, SnapKV, PyramidKV），量化类（KVQuant, KIVI），结构共享类（MiniCache）。所有方法基于相同长上下文解码器（32k令牌预填窗口），压缩预算在LongBench子集调整后固定。
- **FreqDepthKV配置**：默认层块B=4（首尾四层B=2），路由探针每提示128个查询位置，目标压缩比3.8×。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量及训练时长。所有方法仅推理时应用，无需重新训练，因此算力消耗主要来自预填阶段的路由开销和推理阶段的解码加速。文中提到“轻量级在线探针”和“固定DCT变换”，但未量化具体硬件资源。因此，从论文中无法获知确切的算力投入。

## 5. 实验数量与充分性

- **主实验**（表1）：在9个数据集（或任务系列）上评估任务指标和系统指标，对比8种基线方法，结果全面。
- **消融实验**（表2）：系统性地移除各个关键组件（深度-频率分解、稀疏残差、在线路由、重建感知损失、精确模式、层块大小）共9种变体，显示各组件均贡献显著性能提升。
- **额外的配置实验**：测试了不同层块大小（B=2,4,8）和共享深度仅变体。
- **充分性评估**：实验覆盖了长上下文典型任务类型（检索/QA、摘要、代码），消融实验针对设计核心进行了逐项验证，结果客观、公平（所有方法使用相同基础模型和评估协议）。但未涉及多轮对话、多模态等更复杂场景，也未与其他深度共享方法（如更细粒度的Merge或Adaptive approach）进行全面比较。

## 6. 论文的主要结论与发现

- FreqDepthKV在几乎所有任务指标上优于所有压缩缓存基线，且逼近全量KV性能：EM 58.3（全量58.7）、F1 63.0（全量63.4）、ROUGE-L 32.5（全量32.8）、pass@1 48.1（全量48.6）。
- 系统效率显著提升：解码吞吐70.4 tokens/s（全量38.2），TTFT 2.06s（全量2.91s），峰值内存6.2 GB（全量24.0 GB），有效压缩比3.9×。
- 相比于非深度频率方法（如MiniCache），在同等甚至更低内存下提升了所有任务指标；相比于强令牌选择基线PyramidKV，F1提升1.8点，pass@1提升2.3点，内存减少1.2 GB。
- 消融证实：深度-频率分解、稀疏残差、在线路由、重建感知损失和精确模式各自对稳健性不可或缺；共享深度仅变体性能最差，表明单纯的低频共享不足以保护检索证据；层块大小B=4（边界层B=2）为最佳平衡点。

## 7. 优点

- **新颖的频率引导深度分解**：将深度冗余视为频率结构化信号，而非均匀共享，保留关键高频信息。
- **自适应路由无需重训练**：在线探针根据提示结构动态为每个头选择模式，轻量且通用。
- **重建感知损失**：直接基于注意力logits变化衡量压缩影响，比仅基于令牌重要性的方法更精准保护证据。
- **系统效率与准确率兼得**：在极低内存（6.2GB）下仍保持接近全量KV的准确率，解码吞吐大幅提升。
- **兼容性强**：正交于令牌级和精度级压缩，可直接结合使用。
- **实验设计公平**：控制相同基础模型、评估协议，公平对比多种代表性方法。

## 8. 不足与局限

- **路由固定**：路由决策仅在预填阶段做出，解码过程中不更新，可能无法适应生成长序列时依赖关系的动态变化。论文在未来工作中提及了此局限。
- **未与其他最新深度共享方法全面比较**：只对比了MiniCache，未与KVSharer、TailorKV等更近期的层间共享方法对比。
- **未探索与量化结合的联合优化**：论文提到可兼容量化，但未实验联合效果。
- **计算开销未详细量化**：路由探针需在预填阶段为每个头计算三种模式损失，虽声称轻量，但未给出实际额外延迟或FLOPs数据。
- **实验仅覆盖单轮长上下文任务**：未评估多轮对话、RAG场景或多模态任务，泛化性有待验证。
- **模型规模单一**：所有实验基于同一种长上下文解码器，未在不同模型系列（如Llama、Mistral）或不同参数量级上验证。
- **潜在偏差风险**：路由探针依赖采样查询位置，可能对长尾证据不敏感；高频残差选择基于最大logit扰动，可能偏向高注意力分数而忽略低调但关键的交互。文档未进行鲁棒性分析或对抗测试。

（完）
