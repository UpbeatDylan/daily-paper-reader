---
title: "PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs"
title_zh: PersistentKV：面向商用GPU上长上下文LLM服务的页面感知解码调度
authors: Muhammad Ahmed
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26666v2"
tags: ["query:pic"]
score: 8.0
evidence: 长上下文服务中的KV缓存移动优化
tldr: 长上下文LLM服务中KV缓存移动成为主要瓶颈，现有paged-attention调度未充分利用GPU。提出PersistentKV，一种原生块表解码注意力引擎，通过KV-head组映射和紧凑工作队列调度，仅执行非空任务。在RTX 3060上，相比FlashInfer，B8长上下文吞吐提升1.04x-1.08x，B1提升1.40x，同时将启动扇出从16降至2。表明工作分配是决定性服务变量。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26666v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1627, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1645, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1138, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1557, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1054, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1075, \"height\": 224, \"label\": \"Table\"}]"
motivation: 长上下文LLM解码中KV缓存移动成为瓶颈，现有paged-attention调度存在GPU利用率低和混合序列长度冲突问题。
method: 提出PersistentKV，基于原生页表的分组查询注意力引擎，通过KV-head组映射和紧凑工作队列调度非空任务。
result: RTX 3060上校准策略选择FlashInfer或PersistentKV，B8吞吐提升1.04x-1.08x，B1提升1.40x，启动扇出降至2。
conclusion: 工作分配是长上下文LLM服务中的决定性变量，PersistentKV通过页感知调度显著提升吞吐并减少开销。
---

## 摘要
自回归大语言模型（LLM）服务日益受限于键值（KV）缓存移动，而非稠密矩阵乘法。现代分页注意力系统减少了碎片化，成熟的核函数如FlashInfer提供了高度优化的解码注意力。然而，最佳的单核函数实现并非总是最优的服务调度：低活跃度的长上下文解码可能未充分利用GPU，而混合序列长度则在大量精确长度的启动与粗粒度的填充批次之间引入矛盾。我们提出了PersistentKV，一个原生的块表解码注意力引擎，以及针对分组查询注意力（GQA）的页面感知调度研究。PersistentKV按KV头组映射工作，直接在原生页表上执行，并添加了一个紧凑的工作队列调度，仅执行非空的行-KV头-序列-分割任务。在RTX 3060（FP16，页面大小16，Hq=32，Hkv=8，d=128）上，一个经过校准的屋顶线风格策略选择FlashInfer用于小型活跃批次，PersistentKV序列分割用于批次大小1（B1）的长上下文步骤，以及PersistentKV工作队列调度用于支持的B8长上下文GQA步骤。在成本模型参数固定在校准轨迹上的情况下，五个保留的种子在B8双峰、均匀和类Zipf工作负载上平均提高了1.04倍到1.08倍的解码令牌吞吐量，在B1分桶轨迹上提高了1.40倍。对于B4边界情况和未校准的GQA比例，该策略通过路由到FlashInfer避免了性能退化。我们还报告了一个注意力加MLP时序代理和工作负载计数器，显示工作队列调度将保留双峰B8上每步启动次数从16.00减少到2.00。这些结果表明工作分配是一个决定性的服务系统变量。

## Abstract
Autoregressive large language model (LLM) serving is increasingly limited by key-value (KV) cache movement rather than dense matrix multiplication. Modern paged-attention systems reduce fragmentation, and mature kernels like FlashInfer provide highly optimized decode attention. However, the best single-kernel implementation is not always the best serving schedule: low-active long-context decode can under-utilize GPUs, while mixed sequence lengths introduce tension between many exact-length launches and coarse padded batches.   We present PersistentKV, a native block-table decode attention engine and page-aware scheduling study for grouped-query attention (GQA). PersistentKV maps work by KV-head group, executes directly over native page tables, and adds a compact workqueue schedule executing only non-empty row-KV-head-sequence-split tasks.   On an RTX 3060 (FP16, page size 16, Hq=32, Hkv=8, d=128), a calibrated roofline-style policy selects FlashInfer for small active batches, PersistentKV sequence splitting for batch size 1 (B1) long-context steps, and PersistentKV workqueue scheduling for supported B8 long-context GQA steps. With cost-model constants fixed on calibration traces, five held-out seeds improve mean wall decode-token throughput by 1.04x to 1.08x on B8 bimodal, uniform, and Zipf-like workloads, and by 1.40x on a B1 bucketed trace. For the B4 boundary case and uncalibrated GQA ratios, the policy avoids regressions by routing to FlashInfer. We also report an attention-plus-MLP timing proxy and workload counters showing workqueue scheduling reduces launch fan-out from 16.00 to 2.00 launches per step on held-out bimodal B8. These results show that work assignment is a decisive serving-system variable.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）服务在解码阶段的主要瓶颈已经从计算密集型矩阵乘法转变为键值（KV）缓存的数据移动。尽管现有的分页注意力（paged-attention）系统（如FlashInfer）提供了高度优化的内核，但**单一的最佳内核实现并不总是最优的服务调度**：低活跃度的长上下文解码会令GPU利用率不足，而混合序列长度则导致精确长度启动次数过多与粗粒度填充批次之间的冲突。
- **整体含义**：论文提出一种**页面感知的调度方案**，将工作分配（work assignment）本身视为决定服务性能的关键变量，而不仅仅是关注注意力数学运算。通过自适应地选择不同调度策略，可以在商用GPU上显著提升长上下文解码的吞吐量。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：设计一个原生块表解码注意力引擎（PersistentKV），它直接在服务系统的页表上执行，并引入紧凑工作队列调度，仅发射非空的行–KV头–序列分割任务，从而避免填充和多余启动。
- **关键技术细节**：
  - **分组查询注意力（GQA）映射**：将协作线程阵列（CTA）按 (请求, KV头, 序列分割) 映射，一个KV头对应多个查询头（G=4）。
  - **原生块表执行**：KV页面以 `[物理页书, H_kv, 页面大小, d]` 存储，每个请求有一张块表；注意力循环处理32个token的瓦片，使用在线softmax精确计算。
  - **序列分割与行局部边界**：将序列分为S个不相交区间，每个区间计算局部softmax状态，然后合并内核精确合并。使用真正的每行有效长度 `N*_i` 推导瓦片计数、分割范围等，空分割CTA返回中性状态。
  - **紧凑工作队列调度**：对每个活动行i、KV头g、行内分割s，仅当瓦片区间非空时发射一个任务；CUDA网格是一维任务列表；合并内核执行分段softmax归约。这消除了长度分桶带来的多个启动，保持一个路由桶。
  - **校准成本模型**：轻量级屋顶线风格策略，估计每个路由的耗时（考虑KV字节、CTA工作、启动开销、合并开销），选择最佳路由。硬限制：仅在G=4、上下文≥16K token时启用PersistentKV。B4默认路由到FlashInfer以避免退化。

#### 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - 合成轨迹：分桶轨迹（prompt长度8K/16K/32K/64K）、同质轨迹（32K）、双峰轨迹（1个长prompt对3个短prompt）、均匀轨迹、Zipf轨迹。这些轨迹分别测试不同因素：上下文长度、活跃批次大小、长度杂乱度。
  - 还使用一个可再分的CSV轨迹夹具（32个请求，最大活跃8）。
- **Benchmark**：对比相同页面分配、相同GQA形状（Hq=32, Hkv=8, G=4, d=128, FP16, 页面大小16）下的解码吞吐量。主要基线：FlashInfer 0.2.5，此外还有vLLM 0.6.4.post1、TensorRT-LLM 0.8 MMHA、重排+PyTorch SDPA。
- **评价指标**：同步墙时解码token吞吐量、CUDA事件时间、最大误差/平均误差（正确性阈值<2e-3）。还报告了调度器推导的工作负载计数器（启动次数、合并流量等）。

#### 4. 资源与算力

- 唯一指定的硬件：**NVIDIA RTX 3060** (12 GB VRAM, 28 SMs, 实测流读取带宽约331 GB/s)。
- 软件环境：CUDA 12.1, PyTorch 2.5.1。
- 未提及训练时长（仅推理阶段测试），也未使用多GPU或多机器。

#### 5. 实验数量与充分性

- **实验数量**：包括：
  - 孤立原生分页基准测试（表1）：4种方法×3种上下文长度。
  - 主要服务结果（表2）：5个保留种子在4种轨迹上的吞吐量比率（B1分桶、B8双峰/均匀/Zipf），以及B4边界情况。
  - 模型级时序代理（表3）：5个保留种子在B8双峰轨迹上添加合成MLP尾。
  - 调度器计数器（表4）：三种引擎的启动数/CTA工作/合并流量。
  - Nsight Compute硬件计数器（表5）：短轨迹上的内核时间、利用率等。
  - 分割数敏感性（表7）、GQA/MQA扫描（表8）、杂乱度消融、实现消融（因子索引、行局部边界、紧凑队列等）。
- **充分性评估**：
  - **优点**：使用了多个保留种子和不同轨迹分布，进行了正确的数值检验，提供了详细硬件计数器，消融实验覆盖了关键设计选择。
  - **不足**：全部为合成轨迹，缺少真实生产流量；仅在一个GPU上验证；基线版本固定（可能随更新变化）；没有完整的端到端LLM服务栈（缺少预填充、采样、网络等）。作者自己也指出了这些威胁。

#### 6. 论文的主要结论与发现

- **主要结论**：页面感知的调度可以显著提升商用GPU上长上下文解码的吞吐量，但需要根据活跃度和长度杂乱度自适应地选择内核。
- **具体发现**：
  - 在B1（单一活跃序列）长上下文下，PersistentKV的序列分割使墙时吞吐量提升**1.403倍**（±0.065）。
  - 在B8双峰/均匀/Zipf轨迹下，紧凑工作队列调度使墙时吞吐量提升**1.044–1.080倍**；加上MLP尾后提升**1.105倍**。
  - 紧凑工作队列将每步启动数从FlashInfer的1次或分桶的16次降低到**2次**，合并流量也从4.06 MB降至2.54 MB。
  - B4边界和未校准GQA（G=1或8）时，路由到FlashInfer避免退化。
- **系统意义**：工作分配（work assignment）是决定性变量，不仅仅是注意力数学运算。

#### 7. 优点

- **方法创新性**：将原生块表执行与紧凑工作队列调度结合，减少了启动开销和填充浪费，同时保持精确注意力。
- **实验严谨性**：使用校准成本模型，固定参数于一个种子，在5个保留种子上验证，确保非过拟合；进行正确性验证（最大误差<6.1e-5）；报告了CUDA事件时间和墙时时间；提供了Nsight硬件计数器作为补充证据。
- **清晰的问题界定**：明确指出哪些场景有收益（B1, B8 G=4长上下文）、哪些场景需要回退（B4, 其他GQA）。
- **开源/可重现**：提供了外部轨迹文件接口和示例夹具，代码可复现（文中未直接提供但声称在Calibration上有公开工件）。

#### 8. 不足与局限

- **实验覆盖**：仅使用合成轨迹，缺少真实生产流量（如流量到达模式、置换策略、模型层间交互）。仅在一个GPU（RTX 3060）上测试，未涉及A100/H100等数据中心GPU。
- **系统完整性**：实验仅包含解码循环，没有预填充、采样、CPU请求队列、网络开销等完整服务栈；虽然有一个MLP尾代理，但还不是完整Transformer。
- **版本风险**：基线（FlashInfer, vLLM, TensorRT-LLM）版本固定，未来版本可能改变结论。
- **未探索的维度**：未评估不同页面大小、异构页面大小、旋转位置编码（RoPE）融合、解量化注意力等。
- **成本模型局限性**：参数需针对每种GPU重新校准，不是可移植的硬件不变量；且B4附近分割扫描没有产生稳健收益。
- **GQA支持有限**：当前仅G=4有收益，G=1和G=8被门控到FlashInfer，虽然避免了退化但未能扩展到其他常见架构。

（完）
