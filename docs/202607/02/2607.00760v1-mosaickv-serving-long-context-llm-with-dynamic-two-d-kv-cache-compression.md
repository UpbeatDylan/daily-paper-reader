---
title: "MosaicKV: Serving Long-Context LLM with Dynamic Two-D KV Cache Compression"
title_zh: MosaicKV：使用动态二维KV缓存压缩服务长上下文大语言模型
authors: "Sheng Qiang, Ruiwei Chen, Yinpeng Wu, Jinyu Gu, Zhichao Hua, Yubin Xia, Binyu Zang, Haibo Chen"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00760v1"
tags: ["query:pic"]
score: 9.0
evidence: 面向长上下文LLM服务的动态二维KV缓存压缩
tldr: "长上下文LLM服务中KV缓存随上下文线性增长，导致GPU内存耗尽、批处理大小减小和吞吐量下降。现有压缩方法仅针对单维度，压缩双维度会导致精度损失。为此，MosaicKV提出动态二维压缩，根据KV向量中元素重要性非均匀分布，以缓存段粒度选择压缩策略，并利用闲置GPU/CPU资源管理压缩缓存以加速注意力计算。实验表明，相比未压缩基线，MosaicKV实现高达16倍注意力加速、4.8倍解码延迟降低、7.3倍吞吐量提升，内存减少3倍，平均精度损失仅1.76%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00760v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1822, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 875, \"height\": 931, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1571, \"height\": 795, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 869, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1833, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 844, \"height\": 519, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1841, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00760v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 937, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00760v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 194, \"label\": \"Table\"}]"
motivation: 解决长上下文LLM服务中KV缓存内存占用过大导致的性能瓶颈问题。
method: 提出动态二维KV缓存压缩系统，以缓存段粒度识别重要元素并选择压缩策略，同时利用闲置GPU/CPU管理压缩缓存加速计算。
result: "在H800 GPU上多个LLM评估，实现16倍注意力加速、4.8倍解码延迟降低、7.3倍吞吐量提升，内存减少3倍，精度损失仅1.76%。"
conclusion: MosaicKV通过动态二维压缩有效提升长上下文LLM服务效率，内存和性能显著优化且精度损失小。
---

## 摘要
长上下文大语言模型服务现在支持数十万到数百万个令牌的提示，使得键值（KV）缓存成为首要的服务成本。由于缓存随上下文长度线性增长，它会耗尽GPU内存，迫使批处理规模减小，并降低服务吞吐量。现有的KV缓存压缩技术通常只针对序列维度或通道维度，在上下文窗口扩展时，这留下了有限的优化空间。同时压缩两个维度有望实现更高的内存减少，但直接应用这两种压缩形式会导致显著的精度损失。本文介绍了MosaicKV，一种用于极长上下文服务的动态二维KV缓存压缩系统。MosaicKV利用动态二维压缩来解决精度挑战，利用KV缓存内元素的重要性分布非均匀性。MosaicKV并非全局应用一种压缩模式，而是为每个KV向量识别重要元素，并以KV缓存段为粒度选择压缩策略。为了解决性能挑战——细粒度稀疏性和压缩管理开销可能抵消压缩带来的收益——MosaicKV引入了压缩KV缓存管理。该机制利用未充分利用的GPU和CPU资源来维护压缩的KV缓存并加速注意力计算。在H800 GPU上使用多个大语言模型的评估表明，与未压缩的基线相比，MosaicKV实现了高达16倍的注意力加速、4.8倍的解码延迟降低和7.3倍的吞吐量提升。同时，它减少了3倍的内存使用，在LongBench和RULER上的平均精度损失仅为1.76%。

## Abstract
Long-context LLM services now sustain prompts with hundreds of thousands to millions of tokens, making the key-value (KV) cache a first-order serving cost. Because the cache grows linearly with context length, it can exhaust GPU memory, force smaller batches, and reduce serving throughput. Prior KV cache compression techniques typically target only the sequence dimension or only the channel dimension, which leaves limited headroom as context windows scale. Compressing both dimensions promises higher memory reduction, but applying the two forms of compression directly leads to significant accuracy loss.   This paper introduces MosaicKV, a dynamic two-D (dimensional) KV cache compression system for extremely long-context serving. MosaicKV uses dynamic two-D compression to address the accuracy challenge, exploiting the non-uniform importance distribution of elements within the KV cache. Instead of applying one compression pattern globally, MosaicKV identifies important elements for each KV vector and selects compression strategies at the granularity of KV cache segments. To address the performance challenge, where fine-grained sparsity and compression management overhead can offset the gains from compression, MosaicKV introduces compressed KV cache management. This mechanism uses underutilized GPU and CPU resources to maintain compressed KV caches and accelerate attention computation.   Evaluation on an H800 GPU with multiple LLMs shows that MosaicKV delivers up to 16x attention speedup, 4.8x lower decode latency, and 7.3x higher throughput than the uncompressed baseline. At the same time, it reduces memory usage by 3x and incurs only 1.76% average accuracy loss on LongBench and RULER.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）推理中，KV缓存随上下文长度线性增长，成为内存和性能的主要瓶颈。例如，128K上下文下8个并发请求需要128GB KV缓存，1M上下文则达1TB，远超模型权重。这导致GPU内存耗尽、批处理被迫减小、解码延迟增加、吞吐量下降。
- **现有方法局限**：已有压缩技术通常只针对**序列维度**（保留重要token）或**通道维度**（保留重要通道）之一，单一维度难以随上下文规模扩展。同时压缩两维度（二维压缩）理论上可更高程度减少内存，但简单叠加会导致严重精度损失（如70%通道压缩叠加序列压缩后精度损失达82.8%）。
- **本文目标**：提出MosaicKV，一个动态二维KV缓存压缩系统，在极长上下文服务中实现**低内存、高性能、低精度损失**。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用KV缓存中元素重要性分布的**非均匀性**，采用**动态二维压缩**，包括：
  - **逐向量元素选择**（per-vector element selection）：每个KV向量独立选择重要元素，而非使用全局统一通道掩码。
  - **动态压缩策略**：将KV缓存划分为多个**段**（segments），每个段应用最合适的压缩策略（通道压缩率、元素分组大小、SVD旋转矩阵等）。
- **两阶段压缩流程**：
  - **阶段1（通道压缩）**：对每个段计算SVD旋转矩阵，旋转K和V矩阵，然后为每个向量保留top-r重要元素（按绝对值）。输入token在prefill阶段压缩，输出token在decode阶段通过**异构双压缩缓冲**增量压缩。
  - **阶段2（序列压缩）**：decode阶段每个注意力层前，将段内token分成小块（如8-16个），计算每块的key均值，用当前query向量计算块分数，选择top-k块进行注意力计算。
- **压缩KV缓存管理**（解决性能挑战）：
  - **打包稀疏注意力**（Packed Sparse Attention）：将稀疏的KV缓存打包为稠密格式（packed KV format），利用未充分利用的CUDA core（而非Tensor core）直接在打包格式上计算注意力，减少GPU缓存缺失。
  - **异构双压缩缓冲**（Heterogeneous Double Compression Buffering）：GPU缓冲区临时存储新token的压缩KV，CPU缓冲区异步执行段划分、SVD计算、策略生成等后台操作，不阻塞decode阶段。
  - **增量策略生成**：通过增量更新协方差矩阵，加速SVD旋转矩阵计算和段划分。

## 3. 实验设计

- **数据集/场景**：
  - **LongBench**（多任务长上下文基准，12个子任务）
  - **RULER**（长上下文评估，64K上下文，12个任务）
- **对比方法**：
  - **Full baseline**：无KV压缩，使用FlashAttention（微基准）或vLLM（端到端）。
  - **Quest**：仅序列压缩方法（query-aware token selection）。
- **模型**：LLaMA-3.1-8B、Qwen3-4B、Ministral-3-8B（涵盖不同规模和注意力架构，含GQA）。
- **配置**：默认保留10% token（序列压缩），25%通道（通道压缩），块大小8（MosaicKV）vs 16（Quest），BF16精度。

## 4. 资源与算力

- **GPU**：单块NVIDIA H800（80GB HBM）。
- **算力细节**：未明确说明训练耗时（本工作为推理优化系统，非训练）。表2给出了prefill阶段压缩开销的测量（如64K上下文额外1.0s，占比7.6%；1M上下文额外16.6s，占比0.4%）。未提供训练或大规模集群设置信息，因此无法评估训练端资源消耗。

## 5. 实验数量与充分性

- **实验组数**：
  - 精度试验：3个模型 × 2个基准（LongBench 12子任务 + RULER 12子任务）→ 大量子任务结果（表1）。
  - 微基准：注意力延迟（不同上下文长度、批大小、两种token选择率6.25%和12.5%）；内存占用比较。
  - 端到端性能：解码延迟（TBT）和吞吐量（不同上下文长度）。
  - 消融实验：逐步添加SVD旋转、逐向量选择、动态策略，验证各组件贡献（图9）。
  - prefill阶段开销测量（表2）。
- **充分性评价**：实验覆盖全面，包含了精度、内存、延迟、吞吐量、消融、多种模型和上下文长度。仅对比了Quest一种基线，但该基线是当前序列压缩的先进方法，且其他二维压缩方法无法全生命周期压缩，对比合理。总体充分、客观。

## 6. 主要结论与发现

- MosaicKV相比未压缩基线实现：
  - **注意力加速**最高16倍
  - **解码延迟降低**最高4.8倍（平均3.8倍）
  - **吞吐量提升**最高7.3倍（平均5.5倍）
  - **KV缓存内存减少**3倍
  - **平均精度损失仅1.76%**（LongBench 0.86%，RULER 1.86%）
- 相比Quest（序列压缩）：
  - 注意力加速最高2.4倍
  - 延迟降低1.5倍，吞吐量提升2.3倍
- 消融实验证明逐向量元素选择和动态策略是保持精度的关键，SVD旋转进一步改善。

## 7. 优点

- **方法创新**：首次实现全生命周期二维KV缓存压缩（通道+序列），利用动态分段和逐向量选择克服精度损失。
- **资源高效**：挖掘GPU CUDA core和CPU的闲置计算能力管理压缩，不增加关键路径负担。
- **工程优化**：打包稀疏格式、异步异构缓冲、增量策略生成，解决性能退化问题。
- **兼容性**：与FlashAttention、GQA、MoE、PageAttention等主流优化兼容，且可结合量化。

## 8. 不足与局限

- **实验局限**：
  - 仅对比了Quest一个基线，未与InfiniGen、ThinK、ShadowKV等方法进行端到端比较（但这些方法不能全生命周期二维压缩，故可比性有限）。
  - 只评估了三个模型，未覆盖更大规模模型（如70B+）或MoE架构。
  - 未在所有任务上报告完整方差或置信区间。
- **应用限制**：
  - 当前主要优化decode阶段，prefill阶段加速有限；可通过chunked prefill进一步优化。
  - 仅利用CUDA core进行PackedAttention，若未来HBM带宽极高导致计算成为瓶颈，性能可能受限；作者提到可结合Tensor core与CUDA core作为未来方向。
  - 段划分阈值等超参数可能需针对不同任务调优，泛化性待验证。
- **其他不足**：
  - 未讨论对注意力层数、头数变化的鲁棒性。
  - 未提供开源代码或详细复现配置（但论文给出了算法概览）。

（完）
