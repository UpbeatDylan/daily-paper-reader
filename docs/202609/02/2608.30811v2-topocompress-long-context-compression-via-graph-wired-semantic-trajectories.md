---
title: "TopoCompress: Long Context Compression via Graph-Wired Semantic Trajectories"
title_zh: TopoCompress：基于图连接语义轨迹的长上下文压缩
authors: "Daniel Agyei Asante, Yang Li"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30811v2"
tags: ["query:pic"]
score: 8.0
evidence: 面向长上下文推理加速的无训练模型无关上下文压缩框架
tldr: 长上下文压缩对降低大模型推理成本至关重要，但现有方法易碎片化证据且需额外训练或依赖目标模型。TopoCompress提出训练无关、模型无关的框架，通过选择连贯语义跨度实现压缩：先以稠密加词法相关性和语义加速为跨度打分，再构建融合语义相似性与顺序邻接的混合图，传播查询引导分数。在五个长任务上超过强基线，以4倍更小压缩预算达到可比性能，压缩时间比最快基线低1.41倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.30811v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 323, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30811v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1638, \"height\": 662, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30811v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1535, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30811v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1495, \"height\": 336, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.30811v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 746, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1657, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1610, \"height\": 584, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1238, \"height\": 532, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1233, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1234, \"height\": 534, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1454, \"height\": 771, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1151, \"height\": 545, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30811v2/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1213, \"height\": 290, \"label\": \"Table\"}]"
motivation: 现有长上下文压缩方法易分裂证据、需额外训练或依赖目标模型，导致成本高且泛化受限。
method: 对语义跨度进行查询相关性打分，构建语义相似与顺序邻接的混合图并传播分数，从而选择连贯语义跨度。
result: 在五个长上下文任务上超越强基线，以4倍更小压缩预算达到同级性能，压缩时间比最快基线低1.41倍。
conclusion: 提出了训练无关且模型无关的长上下文压缩框架，为高效推理提供了新思路。
---

## 摘要
长上下文压缩对于降低大型语言模型推理的成本和延迟至关重要。然而，现有方法可能会分割重要证据、需要额外的训练或对齐，并且往往依赖目标模型才能实现有效压缩。我们提出TopoCompress，一种无需训练且与模型无关的框架，通过选择连贯的语义片段来压缩长上下文。TopoCompress首先使用稠密与词汇查询相关性以及语义加速对每个片段进行评分。然后，基于语义相似性和顺序邻接构建一个混合图来连接片段，并在图上传播查询引导的相关性分数。在五个长上下文任务（HotpotQA、2WikiMQA、MuSiQue、Qasper和MultiFieldQA-en）中，TopoCompress始终优于强压缩基线。值得注意的是，TopoCompress在压缩预算比最强基线小4倍的情况下实现了与之相当的性能，并提供比最快基线小1.41倍的压缩时间。

## Abstract
Long-context compression is essential for reducing the cost and latency of large language model inference. However, existing methods can fragment important evidence, require additional training or alignment, and often depend on the target model for effective compression. We introduce TopoCompress, a training-free and model-agnostic framework that compresses long contexts by selecting coherent semantic spans. TopoCompress first scores each span using dense and lexical query relevance together with semantic acceleration. It then constructs a hybrid graph that connects spans based on semantic similarity and sequential adjacency, and propagates the query-guided relevance scores over the graph. Across five long-context tasks-HotpotQA, 2WikiMQA, MuSiQue, Qasper, and MultiFieldQA-en-TopoCompress consistently outperforms strong compression baselines. Notably, TopoCompress achieves performance comparable to the strongest baseline while using a 4x smaller compression budget, and provides a 1.41x smaller compression time over the fastest baseline.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：大型语言模型（LLMs）在处理长上下文时面临两项核心挑战：一是推理成本与延迟随 token 数增长而显著上升；二是上下文越长，模型越难以有效利用分散在其中的关键证据，尤其是当证据位于输入中部时。
- **现有方法的问题**：目前主流的长上下文压缩方法（如 LLMLingua、LongLLMLingua、LLMLingua-2）将压缩视为 token 级剪枝问题，存在三个显著缺陷：
  - **证据碎片化**：逐 token 删除会切断词边界、短语、命名实体或句法单元，导致信息失真，甚至破坏答案实体。
  - **非模型无关性**：需要小型压缩器模型（$M_S$）通过对齐/蒸馏来模仿目标模型（$M_T$）的判断，引入额外训练成本并限制跨模型泛化。
  - **忽略证据间关系**：基于困惑度的打分将 token 视为独立单元，无法建模证据链中中间支持信息的连接关系，在多跳推理场景下尤为不足。
- **核心意义**：论文主张压缩应跳出 token 级独立剪枝范式，转向基于图的、保持连贯证据的语义跨度选择，从而实现真正意义的无训练（training-free）且模型无关（model-agnostic）的压缩。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式与算法流程
- **整体框架**：TopoCompress 将上下文分词为连贯语义跨度（如句子或短段落），构建混合图进行查询引导的相关性传播，再以冗余感知的方式贪心选择证据集。
- **步骤 1：语义跨度构建（Semantic Span Construction）**
  - 将每篇文档切分为连续语义单元 $U = \{u_1,u_2,\ldots,u_N\}$，避免 token 级剪枝造成的碎片化。
- **步骤 2：稠密与词法查询相关性（Dense and Lexical Query Relevance）**
  - 使用冻结的文本编码器 $\Phi$（实例化为 bge-m3，同时提供稠密嵌入与词法权重）计算每个跨度的稠密相关性得分：<!-- 由公式(1)，此处为注释文本：$r_i = \max(0, \text{sim}(e_i, e_q))$，即查询与跨度向量的余弦相似度的非负截断 -->
    $r_i = \max(0,\ \text{sim}(e_i, e_q))$，并做归一化得到 $\hat{r}_i$。
  - 词法相关性：<!-- 由公式(2)，此处为注释文本：计算查询与跨度的共同 token 的 bge-m3 词法权重乘积之和 -->$\ell_i = \sum_{t \in q \cap u_i} w^q_t\, w^{u_i}_t$，同样归一化为 $\hat{\ell}_i$。
  - 查询对齐得分为两者的加权组合：<!-- 由公式(3)，此处为注释文本：$g_i = \mu\hat{r}_i + (1-\mu)\hat{\ell}_i$ --> $g_i = \mu\hat{r}_i + (1-\mu)\hat{\ell}_i$，μ 控制语义匹配与精确词法证据的平衡。
- **步骤 3：语义加速（Semantic Acceleration）**
  - 计算每个内部跨度的“语义轨迹加速度”幅度：<!-- 由公式(4)，此处为注释文本：$a_i = 2e_i - e_{i-1} - e_{i+1}$，$\kappa_i = \|a_i\|_2$ -->$\kappa_i = \|2e_i - e_{i-1} - e_{i+1}\|_2$，用于度量语义变化方向在跨度 $u_i$ 处的转折强度，归一化为 $\hat{\kappa}_i$。
  - 用查询对齐门控加速度：<!-- 由公式(5)，此处为注释文本：$s_i = (1 + \lambda\hat{\kappa}_i)g_i$ -->$s_i = (1+\lambda\hat{\kappa}_i)\,g_i$，保证只有“既查询相关又处于语义转折处”的跨度被加强。
- **步骤 4：跨度拓扑与相关性传播（Span Topology and Relevance Propagation）**
  - 构建综合图：语义边（基于嵌入余弦相似度的 k 近邻，权重为相似度正值）加顺序边（同文档内相邻跨度，权重为 1）；邻接矩阵 <!-- 此处为注释文本：$W = \alpha W_{\text{sem}} + \beta W_{\text{seq}}$ -->$W = \alpha W_{\text{sem}} + \beta W_{\text{seq}}$。
  - 行归一化得到转移矩阵 $P = L^{-1}W$，执行个性化相关性传播：<!-- 由第三节内容，此处为注释文本：$s^{(m+1)} = (1-\eta)s + \eta P^\top s^{(m)}$，直到收敛 -->
    $s^{(m+1)} = (1-\eta)\,s + \eta\,P^\top s^{(m)}$，重启项 $(1-\eta)s$ 使分数锚定于原始查询门控信号。传播使本身与查询匹配度不高但与其他证据相连的中间支持信息获得更高的排名。
- **步骤 5：冗余感知的证据选择（Redundancy-Aware Evidence Selection）**
  - 贪心迭代选择：<!-- 由3.6节内容，此处为注释文本：$u^* = \arg\max_{u_i \in R} [\,\tilde{s}_i - \delta \max_{u_j \in A} \text{sim}(e_i,e_j)\,]$ -->$u^* = \arg\max_{u_i\in R}\ \big[\tilde{s}_i - \delta\cdot\max_{u_j\in A}\text{sim}(e_i,e_j)\big]$，对新选跨度中与已选证据在语义上高度重复的候选施加惩罚；选满预算 $K$ 后将跨度还原为文档中的原始顺序。
- **控制器扩展**：还提出在预算用至一半时可由 LLM 控制器判断证据是否充分（suffices），若不足则生成后续问题重新指导剩余跨度的评分，最多调用两次以减少 API 开销。

### 3. 实验设计：数据集、Benchmark 与对比方法
- **Benchmark**：LongBench 的五个英文长上下文问答任务——
  - **HotpotQA**（多文档多跳问答，平均输入约 9,151 词）
  - **2WikiMQA**（多文档多跳，约 4,887 词）
  - **MuSiQue**（多跳问答，约 11,214 词）
  - **Qasper**（科学论文问答，约 3,619 词）
  - **MultiFieldQA-en**（多领域长文问答，约 4,559 词）
- **目标模型**：涵盖闭源模型 GPT-5-mini，以及开源模型 Llama-3.1-8B 与 Qwen3-8B，以验证模型无关性。
- **对比基线**：三种代表性长上下文压缩方法——
  - LLMLingua（困惑度驱动的 token 级压缩）
  - LongLLMLingua（查询感知的对比困惑度方法）
  - LLMLingua-2（训练型 token 分类压缩方法，基于 LLM 蒸馏数据训练双向 Transformer）
- **压缩预算**：$K \in \{2000,\ 1000,\ 500\}$ 三种上下文 token 预算，$K=500$ 为最激进压缩。
- **评测指标**：各任务用 F1，汇总为样本加权平均 F1。

### 4. 资源与算力
- **论文未明确说明 GPU 型号、数量、训练时长或推理集群配置**。仅提及 TopoCompress 的编码器 bge-m3 是冻结的（无需训练）；压缩耗时在任务级以“分:秒”给出，但没有披露具体硬件环境。控制器实验用 GPT-5-mini 作为控制器，有 API 调用开销限制（每样本至多两次），但同样未给出具体硬件/API 成本明细。

### 5. 实验数量与充分性
- **主实验**：3 个目标模型 × 3 个压缩预算 × 5 个数据集，每个数据点对应完整压缩+推理流程，覆盖规模较为充分。
- **压缩时间对比**：在 $K=1000$ 下对比了四种方法在五个任务上的完整压缩耗时。
- **控制器变体实验**：在 3 个模型 × 3 个预算条件、5 个任务上评估了“TopoCompress + 控制器”的效果，并附带分析了对上下文长度的影响。
- **消融实验**：
  - 移除图传播（w/o Graph）：在 HotpotQA、2WikiMQA、MuSiQue 三个多跳数据集上分别用三大模型 × 2 个预算验证（表 8）；
  - 移除查询相关性（w/o Query Rel.）和移除语义加速（w/o Accel.）：以 GPT-5-mini 为目标模型在全部 5 个任务 × 2 个预算下验证（表 9）。
- **定性分析**：基于 MuSiQue 的一个案例展示图传播如何恢复非直接匹配但属于黄金证据链的跨度（表 3）。
- **关于充分性与公平性**：实验具有多维覆盖（模型类型、预算量级、任务类型），且基线均为当前强代表，比较公平；性能与效率两者兼报。但从严谨角度，缺少极长上下文（如论文中未覆盖百万 token 级）和多语言/非问答类压缩任务的验证；控制器实验只在 K=1000 下报告了上下文长度而不含其他预算；消融只深入做了图传播的效果，其余组件只在单个目标模型上验证——这些环节的覆盖可以更全面。

### 6. 论文的主要结论与发现
- TopoCompress 在所有评估的目标模型和压缩预算下一致优于 LLMLingua、LongLLMLingua 与 LLMLingua-2；在 $K=500$ 的最激进预算下，相较最强基线 LongLLMLingua 平均 F1 提升：GPT-5-mini 上提升约 8.15 点，Llama 3.1-8B 上约 8.44 点，Qwen3-8B 上约 7.21 点。
- **压缩效率突出**：TopoCompress 在 $K=500$ 时的性能与 LongLLMLingua 在 $K=2000$ 时相当（GPT-5-mini 上 51.37 vs. 51.44），以 4 倍更小的压缩预算达到同级性能。
- **速度优势**：整体压缩耗时低于 7 分钟，相比最快基线 LLMLingua-2 加速约 1.41 倍；相比 LLMLingua/LongLLMLingua 的约 44/52 分钟，加速更为显著。
- **图传播的价值**：消融验证了移除图传播在 HotpotQA、2WikiMQA 和 MuSiQue 上普遍带来性能下降（最高相对下降 14.1%），说明图传播对保持跨文档、多跳证据链有着关键作用。
- **查询相关性不可或缺**：移除稠密+词法查询相关性导致最大性能下滑，证明查询门控是证据选择的核心信号。
- **控制器的加值有限但存在**：LLM 控制器变体通过中途判定+补充生成式查询可将上下文平均缩减至约 681–857 token（原预算 1000），并带来略微性能提升，说明 TopoCompress 本身的图机制已能恢复大部分中间证据。

### 7. 优点
- 首次将长上下文压缩形式化为基于混合图的“相关性传播”问题，提供了一个 token 级剪枝之外的新范式。
- **无训练且模型无关**：无需蒸馏、无需调整目标模型，冷启动即可适配闭源与开源多种架构，实用性强。
- **证据完整性**：以语义跨度为最小保留单元，配合图传播与冗余惩罚，避免了实体碎片化并可恢复间接证据链，对多跳 QA 尤其有价值。
- **效率显著**：压缩速度快于所有基线，工程成本低，适合 RAG 等对延迟敏感的长上下文流水线。
- 实验覆盖面较广：模型（闭源+开源）× 预算（正常到极端）× 任务（多文档多跳、科学文献、多领域）多维组合，提供了较有说服力的对比证据。

### 8. 不足与局限
- **算力信息缺失**：未披露任何实验所用 GPU 型号、数量或能耗细节，无法评估方法在部署端的绝对资源门槛。
- **任务覆盖不全**：仅在 QA 型任务上评估，未涉及摘要、代码、few-shot 指令、对话等多类长上下文任务；也未探索多语言输入。
- **超参数敏感性空间模糊**：α、β、η、λ、μ、δ、邻居数 k 等超参数如何设置、随任务/预算如何调整，文中未提供系统分析。
- **控制器变体的结论有限**：控制器实验仅以 GPT-5-mini 作为控制器，未评估其他控制器模型；在 $K$ 变化下对“压缩长度节省”的报告不完整。
- **缺乏与长上下文 RAG/检索的联合对比**：没有直接对照检索式方案或“检索+压缩”级联系统，对真实流水线可借鉴性评估还不够。
- **分析仍以局部 F1 为主**：未更深入测量压缩后文本的可读性、事实一致性或对目标模型在开放生成中的行为影响；潜在的选择偏差与评测偏差风险也未展开讨论。
- 文中显示部分数据集上（如 Qwen3-8B 在 MuSiQue 上）TopoCompress 各预算下 F1 差距很小甚至相同，说明在弱推理模型上方法增益受制于下游模型能力，此现象未被深入解释。

（完）
