---
title: Adaptive KV Cache Reuse for Fast Long-Context LLM Serving
title_zh: 自适应KV缓存重用技术用于快速长上下文LLM服务
authors: "Fei li, Song Liu, Yan Liu, Jinhua Cui, Shiqiang Nie, Jinyu Wang, Weiguo Wu"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.24022v1"
tags: ["query:pic"]
score: 9.0
evidence: 自适应KV缓存重用直接针对非前缀（位置无关）KV重用
tldr: 长上下文LLM推理中，prefill阶段的TTFT延迟是主要瓶颈。现有KV缓存重用局限于严格前缀场景，非前缀重用会破坏全局注意力并引入异构I/O开销。本文提出CacheTune，通过频域分析离线识别语义关键KV对，在线选择性重计算并结合稀疏传输、异步重叠、延迟位置编码恢复及自适应重计算比率，平衡计算与I/O。在主流LLM上实现3.72x-4.86x TTFT加速和3.93x-6.21x吞吐提升，生成质量接近完全重计算，即使卸载至SSD/HDD仍保持2.34x-2.36x加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24022v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1468, \"height\": 866, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1587, \"height\": 1018, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1502, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1780, \"height\": 1151, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 826, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 877, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 872, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 877, \"height\": 367, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24022v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1744, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24022v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 819, \"height\": 378, \"label\": \"Table\"}]"
motivation: 非前缀场景下KV缓存重用破坏全局注意力，且GPU外部缓存池的I/O开销成为新TTFT瓶颈。
method: 离线频域分析识别关键KV对，在线选择性重计算；结合稀疏传输、多流异步重叠、延迟位置编码恢复及自适应重计算比率。
result: TTFT加速3.72x-4.86x，吞吐提升3.93x-6.21x，生成质量接近全重计算；SSD/HDD场景下加速2.34x-2.36x。
conclusion: CacheTune实现高效非前缀KV缓存重用，兼顾语义一致性与异构I/O优化，显著降低长上下文推理延迟。
---

## 摘要
在长上下文大语言模型（LLM）推理中，由预填充阶段引起的首令牌时间（TTFT）延迟已成为限制交互性能和部署成本的首要瓶颈。KV缓存重用提供了一条直接减少冗余预填充的路径，但传统前缀缓存仅适用于严格前缀场景；在非前缀设置中直接重用KV缓存会破坏跨块全局注意力关系，并导致生成质量显著下降。当可重用的KV缓存被卸载到GPU外部缓存池时，跨异构硬件层的I/O开销进一步成为新的TTFT瓶颈。因此，高效的非前缀KV缓存重用需要同时解决语义一致性恢复和计算-I/O协同优化问题。

本文提出了CacheTune，一个面向长上下文LLM服务的频率引导且硬件感知的KV缓存重用系统。CacheTune首先通过频域分析离线识别对跨注意力恢复最关键的KV对，然后在线仅选择性重计算这些语义关键令牌，同时重用其余KV。为了将这种语义选择转化为端到端延迟降低，CacheTune进一步结合了稀疏KV传输、多流异步重叠、延迟位置编码恢复以及硬件感知的自适应重计算比率调整，以平衡跨异构缓存池的计算与数据移动。在主流LLM和长上下文任务上的评估表明，CacheTune实现了3.72倍至4.86倍的TTFT加速和3.93倍至6.21倍的吞吐量提升，同时保持生成质量接近完全重计算。即使缓存被卸载到I/O受限的SSD/HDD存储，CacheTune通过自适应重计算仍能维持2.34倍至2.36倍的TTFT加速。

## Abstract
In long-context Large Language Model (LLM) inference, the Time-To-First-Token (TTFT) latency incurred by the prefill stage has become the foremost bottleneck limiting interactive performance and deployment cost. KV Cache reuse offers a direct path to reduce redundant prefill, yet traditional prefix caching applies only to strict-prefix scenarios; directly reusing KV Cache in non-prefix settings breaks the cross-chunk global attention relationships and causes significant degradation in generation quality. When reusable KV Cache is offloaded to GPU-external cache pools, I/O overheads across heterogeneous hardware tiers further emerge as a new TTFT bottleneck. Efficient non-prefix KV Cache reuse therefore requires both semantic-consistency recovery and compute-I/O co-optimization.   This paper presents CacheTune, a frequency-guided and hardware-aware KV Cache reuse system for long-context LLM serving. CacheTune first identifies, offline, the KV pairs most critical to cross-attention recovery through frequency-domain analysis, and then selectively recomputes only these semantic-critical tokens online while reusing the remaining KVs. To turn this semantic selection into end-to-end latency reduction, CacheTune further combines sparse KV transfer, multi-stream asynchronous overlap, deferred positional-encoding recovery, and hardware-aware adaptive recomputation-ratio tuning to balance computation and data movement across heterogeneous cache pools. Evaluations on mainstream LLMs and long-context tasks show that CacheTune achieves 3.72x-4.86x TTFT speedup and 3.93x-6.21x higher throughput while maintaining generation quality close to full recompute. Even when caches are offloaded to I/O-bound SSD/HDD storage, CacheTune sustains 2.34x-2.36x TTFT speedup through adaptive recomputation.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：长上下文LLM推理中，预填充阶段带来的首令牌时间（TTFT）延迟成为主要瓶颈；现有KV缓存重用仅适用于严格前缀场景，在非前缀（如RAG中动态拼接多个独立文档块）场景下直接重用会破坏跨块全局注意力关系，导致生成质量严重下降。同时，当可重用KV缓存被卸载到GPU外部缓存池（CPU内存、SSD、HDD）时，跨异构存储层的I/O开销成为新的TTFT瓶颈。
- **研究动机**：需要同时解决两个挑战：（1）如何在不完全重计算的情况下准确恢复丢失的跨块语义依赖；（2）如何在异构存储层级中动态平衡重计算开销与数据迁移开销，实现端到端延迟最优。现有方法（CacheBlend、EPIC、KVLink等）缺乏全局语义感知或存在训练开销/模型依赖，且未考虑跨层级的计算-I/O协同优化。

#### 2. 方法论：核心思想、关键技术细节

- **核心思想**：提出 **CacheTune**——频率引导且硬件感知的KV缓存重用系统。利用离线频域分析识别对全局语义恢复最关键的KV对（低频率成分），在线仅选择性重计算这些关键KV对，同时重用其余KV；并通过稀疏传输、多流异步重叠、延迟RoPE恢复和硬件感知自适应重计算比率调整，将语义选择转化为端到端延迟降低。
- **关键技术细节**：
  - **频域选择性重计算**：对每个KV块逐层应用rFFT（实值快速傅里叶变换）转到频域，保留最低α比例（默认0.5）的低频分量，通过逆变换重建后计算每个token的L2能量分数，联合Key和Value分数得到重要性分数，使用TopK选择前r*N个token作为重计算候选集（\( \mathcal{I}_{\text{freq}}^{(\ell)} \)）。
  - **稀疏KV卸载与重用流水线**：利用重计算索引集同时指导I/O加载：只从外部缓存池传输非重计算KV对（\( (1-r)N \)个），减少传输量；通过多CUDA流（传输流、重计算流、默认计算流）异步重叠稀疏传输、在线重计算和延迟RoPE恢复。延迟RoPE：缓存预RoPE的原始Key，在线时根据全局位置应用RoPE，解决位置编码不一致。
  - **硬件感知自适应重计算比率调度**：将每层延迟建模为 \( T^{(\ell)}(r) = \max(r N t_c, (1-r)N t_i) + t_o \)，其中 \( t_c \) 为每token重计算开销，\( t_i \) 为每token有效传输开销，\( t_o \) 为固定流水线开销（通过硬件profiling测量）。通过roofline分析得到理论最优 \( r_0 = t_i/(t_c+t_i) \)，并以 \( r_0 \) 作为先验，使用黄金分割搜索（GSS）在小校准集上最小化平均TTFT，校准成本极低（5-10个样本）。同时设置质量下界 \( r_{\min}=15\% \) 防止低偏好媒体下语义损失。

#### 3. 实验设计

- **数据集与场景**：五个长上下文任务数据集——SAMSum（多轮对话摘要）、MuSiQue（多跳QA）、WikiMQA（多跳QA）、HotpotQA（多跳QA）、Multi-News（多文档摘要）。评估指标：ROUGE-L（摘要）、F1（QA）。
- **Benchmark**：对比7种方法：
  1. Full Recompute（完全重计算）
  2. Full KV Reuse（直接重用，无重计算）
  3. vLLM Prefix Caching（只重严格前缀）
  4. CacheBlend（HKVD驱动，15%重计算比率）
  5. EPIC（固定前16个attention-sink位置重计算）
  6. CacheSlide（加权修正注意力，适配多块设置）
  7. KVLink（训练可学习链接令牌，每块5个令牌）
- **硬件配置**：
  - A100平台：2× NVIDIA A100-40GB，PCIe Gen3 ×16，HDD缓存池（有效读写约205/201 MB/s）。
  - RTX 4090平台：2× NVIDIA GeForce RTX 4090-24GB，PCIe Gen3 ×16，SSD缓存池（有效读写约535/445 MB/s）。
  - 软件：Ubuntu 22.04.1, Linux 6.8.0, PyTorch 2.2.1, CUDA 12.4。

#### 4. 资源与算力

- 论文明确提及使用了 **2× NVIDIA A100-40GB** 和 **2× NVIDIA GeForce RTX 4090-24GB** 两种GPU配置，每个平台均为双卡，通过PCIe Gen3 ×16连接。未说明训练时长，因为CacheTune不需要训练（KVLink需要预训练，但论文未报告其训练成本）。离线频域分析开销极小：对3K token的KV块，生成重计算索引平均约0.69 ms（GPU）。校准搜索约3-4分钟（10个样本）。整体算力需求较低。

#### 5. 实验数量与充分性

- **实验组数**：
  - 主实验（图7）：3个模型 × 5个数据集 × 8种方法 = 120个数据点（实际展示为散点图）。
  - 吞吐量实验（图8）：不同请求速率下TTFT趋势，覆盖多个数据集/模型组合。
  - 重计算比率影响（图9）：Mistral模型，r从5%到25%观察准确率和TTFT。
  - 消融实验（图10）：对比随机、高频、低频三种选择策略，相同比率15%。
  - 存储层级实验（Table 2， Figure 11）：GPU vs CPU vs SSD vs HDD，多种方法对比。
  - 总计约7-8组主要实验图表，覆盖性能、消融、鲁棒性、适应性。
- **充分性与公平性**：
  - 充分：覆盖多种模型尺寸、多种任务类型、多种缓存介质，且对比了当前所有主流非前缀重用方法。
  - 客观公平：对基线方法采用最佳配置（如EPIC和KVLink使用GPU内存以最小化TTFT），CacheTune则使用CPU内存进行公平对比（Table 2）；所有方法在同一硬件环境下运行。消融实验设计合理，验证各组件贡献。
  - 不足之处：未提及是否对基线方法调参或优化使其达到最佳性能，可能对某些基线（如CacheSlide）的适配不完美；未报告统计置信区间或多次运行误差。

#### 6. 主要结论与发现

- **性能提升**：CacheTune在保持生成质量接近完全重计算（平均保留94.8%质量）的同时，TTFT加速3.72×-4.86×，吞吐量提升3.93×-6.21×。
- **与SOTA对比**：在相同15%重计算比率下，CacheTune准确率高于CacheBlend，且TTFT更低；优于EPIC（高精度损失）、KVLink（需训练且精度损失）、CacheSlide（主要应对位置漂移而非全局语义）。相比vLLM Prefix Caching（仅严格前缀），CacheTune充分利用非前缀块。
- **频域分析有效性**：低频选词显著优于随机和高频选词，证实低频成分编码全局语义。
- **自适应重计算比率**：在慢速存储（HDD/SSD）下，自适应提升比率到36.4%/30.9%，将TTFT加速从固定15%的1.92×/2.05×提升到2.36×/2.34×。

#### 7. 优点

- **算法-系统协同设计**：将语义重要性建模（频域分析）与系统优化（稀疏传输、异步流水线、硬件感知调度）紧密结合，实现端到端延迟降低。
- **训练无关且模型无关**：频域重要性得分无需额外训练，可即插即用于不同LLM（已验证Mistral、Llama、Qwen）。
- **低校准成本**：硬件感知调度仅需5-10个样本和3-4分钟profiling，可复用。
- **鲁棒性**：在GPU、CPU、SSD、HDD四种缓存介质下均能保持显著加速，且SSD/HDD场景仍优于基线。

#### 8. 不足与局限

- **极端I/O场景限制**：当外部存储带宽极低或异步重叠窗口不足时，系统可能仍被I/O限制，论文未给出明确边界。
- **实验覆盖**：
  - 未测试更大模型（如70B+）或更长上下文（如128K+），可能影响稀疏传输和流水线效率。
  - 未报告多次运行的标准差或置信区间，统计显著性存疑。
  - 未评估在真实多用户并发场景（如请求调度、缓存竞争）下的性能。
- **潜在偏差**：
  - 低频截断α=0.5为人工选择，虽声称对0.3-0.7不敏感，但未系统验证不同α对最终TTFT和准确率的影响。
  - 校准集仅使用SAMSum样本，可能对长文档场景代表性不足。
- **应用限制**：
  - 依赖离线频域分析，对于动态生成且无法预先计算KV的全新内容无法直接应用。
  - 延迟RoPE要求缓存预RoPE的Key，需修改缓存格式，与现有系统集成需额外适配。

#### （完）
